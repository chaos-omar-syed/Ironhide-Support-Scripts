# Ironhide dashboard — requested changes (2026-09-10 session), ownership & status

Owner A = VIZ agent (plots.py, liveserver.py, pages/1_live.py, icons.py, theme.py)
Owner B = DATA/UX agent (pages/3_data_source.py, pages/2_spec.py [delete], app.py, ih/feed.py, ih/data.py, ih/archive.py [new], track_correlation/quickdump.py refactor)
Owner C = ENGINE agent (ih/engine.py + tests/test_engine*.py)  — must not edit A/B files
Owner D = INTEGRATION (runs after A+B+C): full tests, relaunch, preview, bundle

## Cross-agent CONTRACTS (agree on these; do not rename)
- feed rows (B provides): track rows gain `truth_match_id: str|None`, `truth_match_conf: float|nan`, `contributors: str(JSON)|None`; obs rows gain `truth_target_id: str|None`, `truth_match_type: str|None`, and bistatic fields `bi_rng_m`, `bi_rng_rate_mps` when present (else NaN). Snapshot dict `snap["tx_lla"]` (lat,lon,alt) or None (co-located → None).
- engine output (C provides, A consumes): `A["track_events"]` = list of {"t":epoch,"role":"target"|"interceptor","old":int|None,"new":int|None,"rule":str} newest last (cap 50); `A["tgt_rule"]`, `A["itc_rule"]` = human string ("radar match conf 1.00" / "position gate" / "excluded ADS-B N432R"); `A["cpa"]` semantics unchanged (gated); `A["role_ids"]` = {"target":[ids], "interceptor":[ids]} echo of the Data-source assignment (B stores it in session state key `role_ids`; C reads it via the snapshot param P["role_ids"]).
- UI state (B provides): `st.session_state["mode"] in {"live","archive"}`, `["role_ids"]`, `["mru_number"]`, `["frame_mode"] in {"engagement","follow","fit_flight","fixed"}`.

## A — visualization (plots.py / liveserver.py / 1_live.py / icons.py / theme.py)
A1 meas quad: per-sample target-side gating (ON-TARGET full line, DEPARTED thin .35 alpha; ★ first on-target, ✕ last on-target, "→ departed" glyph)
A2 meas quad: axes = truth envelope ±15% (min spans), off-scale ▲/▼ indicators per track
A3 meas quad: obs visible — filled 6 px #cfd6de @.75 with surface ring, drawn first
A4 meas quad: ★/✕ id pills 12 px mono primary ink (CARD bg, RULE border); legend/titles 12 px
A5 meas quad: z-order obs → departed → on-target → truth → markers
A6 error panel: bridge coast gaps ≤3 s (line lighter .6 alpha, σ interpolated); >3 s = true break; 4 px coast indicator strip per card; caption updated
A7 meas quad: BOTH truths (target red #e66767, interceptor blue #3987e5, 3 px); tracks labelled "#177 · target"/"#203 · interceptor"; legend groups TRUTH/TRACKS/OBS; caption
A8 (viz side) track-change UI: TRACK CHANGES card (last 5 events), TARGET TRACK tile sub-line "prev #129 · changed 07:22:31" + amber edge 10 s, vertical ticks "→ #177" on error panel + meas quad, status line "trk #177"  (consumes A["track_events"])
A9 meas quad: BISTATIC range & range rate via spa.geometry.bistatic_range_m / bistatic_range_rate_mps (TX from snap["tx_lla"], co-located → 2×mono, caption says so); obs bistatic fields when present; axis spans ±0.6 km / ±20 m/s
A10 middle column: one chip row ≤5 (Coverage · Allegiance · Predicted miss · Radar tracks · MAVLink feeds merged); separation figure gets freed height (~65–70% col in 3-col; ~32% in 2-col)
A11 map: track-number pills ("#177" red, "#203" blue) at newest track point, move with ticks, amber flash 10 s on id change
A12 map: default frame = ENGAGEMENT BOX (archive: truth bbox around gated passes ±20 s + current, pad 25%, min hw 500 m; live: recent-action 120 s box with 15% edge hysteresis, 600 ms animate); options fit_flight / fixed; reset pill → default frame
A13 map: reset/frame application computes y range from plot pixel aspect (1:1 px), never sets inconsistent x+y ranges; config.doubleClick=false → our reset; satellite zoom from view m/px (≤1.2 m/px, z≤18), refetch on frame change (debounced), keep old tile until loaded
A14 sidebar/status: source line "● LIVE · MRU 91 · run X" / "● ARCHIVE · 8/28 · Flight 1" (A renders; B supplies state)

## B — data source / app / feed / archive   — STATUS: all DONE (owner B, 2026-09-10 17:40)
B1 DONE  Spec page deleted (pages/2_spec.py, sidebar threshold controls, app.py page entry); pages = Data source (default) · Live. Spec tests removed from tests/test_apptest.py + tests/test_paths.py (spec CSS helpers in theme.py: A's file — see requests).
B2 DONE  Data source is the FIRST tab + default landing (app.py); page-scoped operator typography via `st.container(key="ds_page")` + `.st-key-ds_page` CSS in pages/3_data_source.py — rem only (body/labels 1.05 rem, section headers 1.25 rem, primary values 2.1 rem, primary buttons 3.6 rem tall / 1.2 rem), so A's `--ih-scale` text size applies; theme tokens untouched.
B3 DONE  ONE big mode selector (LIVE RADAR UNIT preselected · ARCHIVE REPLAY; key `_ds_mode_w` -> `ds_mode`), only the selected mode's content renders. Selecting ARCHIVE sets the engine source at once; LIVE re-engages the engine when a run is already followed (else after Connect).
B4 DONE  MRU NUMBER input (`mru_number`) -> `feed.resolve_mru_host` 10.1{NN}.28.205 with the "→ IP" line; Advanced: custom host (wins), port, db, run filter (collection name OR friendly name), auto-assign patterns, history. Big CONNECT (`connect_btn`) -> `feed.connect`: probe + auto-select the newest run with 106/143 data (preferring one written < 10 min ago) and follow immediately (`live_run`, `source/mode=live`).
B5 DONE  Runs: `st.selectbox` (`_run_w`) newest first, label `feed.run_label` = "<name> · <id8> · <start PDT> · <last data age>" (start from the `runs` collection date_time, which is UTC — verified on MRU39: start + run_duration == newest doc); archive flights: selectbox over flights.json (built-in 8/28 + saved), label `D.flight_label` = "8/28 · Flight 1 · 07:19–07:27 · 3 passes". No free-text ids anywhere.
B6 DONE  LIVE INFO panel (fragment, 2 s; 5 s while waiting for feeds): `feed.live_info` — indexed queries over the last 60 s of the run's DATA: last-data age tile (green/amber/red), radar tracks + observations counts, MAVLink feeds table (id, role, rows, first/last, age, ALIVE/STALE/DOWN), host · ping · run · start · antenna · transmitter (tx_lla or "co-located").
B7 DONE  MAVLINK ROLE ASSIGNMENT card (both modes): TARGET / INTERCEPTOR multiselects over the detected ids (prefilled by the patterns until the operator assigns), Swap, an id in both roles is REFUSED (previous assignment kept), applies immediately (`D.set_roles` -> `role_ids` + live buffers re-read); amber "waiting for MAVLink feeds" until an id appears (rescan 5 s; a newly detected id triggers a whole-page rerun).
B8 DONE  Sidebar mode-aware (app.py): LIVE -> `T.source_line_html("live", …)` + unit / run / last-data glyph / TGT-INT mapping + Disconnect (`D.disconnect` -> back to the replay) + Refresh interval; ARCHIVE -> source line + replay transport (+ speed). Display: Screen size, Text size (key `text_scale`, default follows the preset rule, A's spec), Metrics window, Path history, Map frame (`frame_mode`: engagement | follow | fit_flight | fixed; zoom slider enabled for follow/fixed only), Reset, CPA gate, toggles.
B9 DONE  SAVE TO ARCHIVE card (Live): last-N-min / time range (prefilled with the run's MAVLink window via `feed.mavlink_span`, else last 10 min) / whole run; label (default `D.next_flight_label`); output <ARCHIVE_ROOT>/<day>/<run8>_<label>/ {mavlink/, tracks/ (+truth_match_id, truth_match_conf, contributors JSON), obs/obs.csv (t, az, el, rng_m, rr_mps, bi_rng_m, bi_rng_rate_mps, truth_target_id, truth_match_type), meta.json}; flights.json entry appended (8/28 schema + dir/label/source; `D.append_flight_entry` is Streamlit-free for the worker thread); background thread + progress (`ih.archive.start_save/job`, 120 s indexed full_sec chunks, 120 s socket timeout); both 143 layouts via feed.track_rows; completion callout + "Switch to this flight" (`switch_saved`) replays it through the archive path. Core = NEW ih/archive.py (`save_archive`, `dump_window`); track_correlation/quickdump.py is now a thin CLI over it (same layout; `--register` appends flights.json; `--mru NN`).
B10 DONE feed projections: 143 + payload.truth_match / payload.contributors; 103 + observations.truth_target.target_id / .match_type (error_info NOT carried: a dict per obs, nothing consumes it), amb_bistatic_rng_km / amb_bistatic_rng_rate_ms; `tx_lla` derived from 2026.9.x `contributors[].tx_lla_ddm` when it differs from `rx_lla_ddm` by > 5 m (MRU39 publishes TX ≈ 100 m from RX), else None (co-located).
B11 DONE `D.goto_save()` handler (sets ds_mode=live, `_goto_save`, `save_focus`); app.py switches to the Data-source page when `_goto_save` is set and the save card shows the "Save requested from the Live page" callout. A's Live-page "Save…" button currently calls st.switch_page directly — see requests to A.

### B — snapshot / state CONTRACT as implemented (exact names)
- `snap["track_meta"]` = {tid: {"truth_match_id": str|None, "truth_match_conf": float|nan, "contributors": str(JSON)|None, "t": epoch}} — the radar's runtime truth match per track (block 143 payload.truth_match; target_id is a MAVLink id OR an ADS-B callsign e.g. CSG2537; conf parsed from match_type "plurality_based, confidence_0.87"); newest state wins; only tids present in snap["tracks"]. Track arrays stay numeric (13 cols) — the string fields live here.
- `snap["obs"]` (n,8) = (t, az_rad, el_rad, rng_m, rr_mps, bi_rng_m, bi_rr_mps, match_code NaN/1 target/2 interceptor/3 other); `snap["obs_meta"]` = {"truth_target_id": object array str|None, "truth_match_type": object array str|None} ALIGNED row-for-row with snap["obs"] (archive: all None). bi_* NaN when the unit does not publish them (engine falls back to 2 x mono).
- `snap["tx_lla"]` (lat, lon, HAE m) | None (co-located); `snap["tx"]` carries the same value (older name). `snap["unit"]` = "MRU91" (live: from mru_number).
- Session state: `mode` ("live"|"archive", always == `source`; `D.set_source` keeps both), `role_ids` = {"target": [ids], "interceptor": [ids]} (echo of truth_tgt_ids / truth_itc_ids; `D.set_roles`), `mru_number` (int), `frame_mode` (engagement|follow|fit_flight|fixed; `D.FRAME_MODES` labels, `D.frame_mode()`), `ds_mode` (page selector), `text_scale` (label, A's rule), `live_run_name`, `save_focus`.
- fake mongo (tests/fake_mongo.py) for C4: block-143 `truth_match` = None when > 150 m from both drones; MAVLink match {target_id "mav14550_1_1" | "mav14551_2_0", match_type "plurality_based, confidence_0.xx" (conf = 1 - d/150, >= 0.5), source "MAVLINK"} within 150 m; EVERY state of track ids % 13 == 0 -> ADS-B callsign "N432R" (conf 0.90, hard exclude). `contributors` with tx_lla_ddm == rx_lla_ddm (co-located) in RUN_COLL; TRACKS_ONLY_COLL's TX is offset 0.001 deg north (-> tx_lla derived). Obs `truth_target` = mav14550_1_1 / "range_and_el" within 0.06 rad + 300 m of the target truth, every 9th unmatched obs -> "DAL1556" (ADS-B); amb_bistatic_rng_km = 2 x mono, amb_bistatic_rng_rate_ms = -2·amb_dop.

### B — requests to A / C / D
- A: `pages/1_live.py` "Save…" button — please use `on_click=D.goto_save` (then your st.switch_page) so the Data-source page lands on the save card with the "Save requested" callout (`save_focus`). Topbar crumbs are now "… · 1 / 2" (Data source) / "… · 2 / 2" (Live) — 1_live.py still says "1 / 3". Spec CSS helpers (`.ih-spec`, `spec_tiles_html`) in theme.py are dead code now (B1).
- A: `P["role_ids"]` — the Live page builds P for E.analyze; please include `"role_ids": s["role_ids"]` (and `"frame_mode": D.frame_mode()`) so C reads the assignment through the params as the contract says. `D.current_frame()` returns the footprint box only for fit_flight (None for engagement / follow / fixed — the viz derives those).
- C: read the radar match from `snap["track_meta"][tid]` (shape above) and the obs match from `snap["obs"][:, 7]` / `snap["obs_meta"]`; the fake-mongo rules above give MAVLink / ADS-B / null cases deterministically (ADS-B = tids % 13 == 0, e.g. 104, 117, 130, 143, 156, 169, 182, 195, 208 during F1).
- D: AppTest LIMITATION found — `AppTest.switch_page()` runs the target page file STANDALONE (the entry script app.py never runs on a switched page: no sidebar, `_qp_done` unset; widget-bound sidebar keys then reset to defaults on the next run). Tests therefore exercise the sidebar on a landing-page instance and the Live page in a separate `_at(LIVE, **state)` instance (tests/test_paths.py `_live_of`). Any remaining `_at(APP…).switch_page("pages/1_live.py")` rows in tests/test_apptest.py that read the sidebar will hit this.
- D: tests/test_review_fixes.py asserts `archive_obs(...).shape[1] == 5` -> now 8 (D.OBS_COLS); tests/test_liveserver.py asserts the old preset heights (800/1040/560) -> A's 600/860/1180. Both files are outside B's ownership.
- D: run `tests/test_archive.py` (new) in the full suite; the MRU39 rows in tests/test_live_mongo.py need 10.139.28.205 reachable (skipped otherwise).

### B — findings worth knowing
- corr_lib.EnuFrame is equirectangular with 111 320 m/deg for BOTH axes: at 33.75 deg N the meridional degree is ~110 919 m -> truth N is scaled +0.36 % (E +0.1 %): 3-4 m at 1 km, 11 m at 3 km, versus the radar's NED / pymap3d. The live path and the saved archives share this frame (so replay == live), but it is a systematic truth-vs-track range-scale bias worth fixing in corr_lib (not B's file).
- MRU39 `runs.date_time` is UTC; `contributors[].tx_lla_ddm/rx_lla_ddm` exist on 2026.9.x TRACKS payloads (TX ~100 m from RX on MRU39 -> `tx_lla` is derived there, bistatic geometry from the run).
- Real 103 obs carry `truth_target` for ADS-B airliners with `error_info {range_m, …}` (not carried into the snapshot).

## C — engine
C1 track selection precedence: truth_match_id ∈ role ids → priority candidate; truth_match_id not null & not MAVLink → HARD EXCLUDE (ADS-B); null → 150 m position gate; JOINT target/interceptor assignment (distinct tracks, min summed gated distance); hysteresis kept (prev tid if < gate and ≤ 1.5·best + 20 m); rule strings A["tgt_rule"]/A["itc_rule"]  — **DONE (C)** `engine.select_tracks` (+ `role_candidates` / `joint_assign` / `match_role` / `role_ids_of`); reads `snap["track_meta"]` (absent → all null → position gate) and `P["role_ids"]` (absent → `D.role_assignment()` → patterns); priority allowed to 1.5×gate; brute-force top-5×top-5 (+none, cost = one gate) pair search; priority bonus 1e4 > hysteresis bonus 1e3 (a radar match beats a sticky position-gate track); extra: a MAVLink match pointing at the OTHER role is position-gated with a +75 m handicap (duplicate target track fills an empty interceptor slot, loses to any null-match track). Rules: "radar match conf 0.93" / "radar match" (no conf) / "position gate" / "hysteresis" / "excluded ADS-B N432R" (role empty because its nearest in-gate track is an airliner) / "no track in gate". Also `A["excluded_tracks"]` = [{tid, match_id, conf, d, rule}] by distance, `A["role_ids"]` echo, `A["tgt_cands"]/["itc_cands"]/["iscores"]`; excluded tracks never become `alt_tid` and are skipped by `meas_space`. `pick_target_track` kept (apptest helper).
C2 track-change events A["track_events"] (target & interceptor id changes, with rule), session-capped 50  — **DONE (C)** dict events {"t","role","old","new","rule"} newest last, cap 50 in session key `track_events` (legacy tuples converted in place); `A["tgt_change"]/["itc_change"]` are now the event dicts, `tgt_flash/itc_flash` unchanged (10 s, not on first acquisition). State lives in `engine.session_state()` = `st.session_state` under a script-run ctx, else module dict `engine._BARE_STATE` (bare tests / CLI). A-request (additive): `meas_space` track dicts now also carry role-independent `on_tgt` (dT<150 & dT≤dI) and `on_itc` (dI<150 & dI<dT) bool arrays.
C3 CPA gate unchanged (min sep < gate AND rose ≥20 m/15% within 10 s) — verify still holds with joint assignment  — **VERIFIED (C)** CPA block untouched; `test_f1_cpa_gate_unchanged_by_joint_selection` reproduces the apptest sanity points through `analyze()` (07:22:00 → run-min 78.4 m @ 07:21:43, no CPA at 70 m, CPA at 100 m; 07:22:31 → none; 07:22:45 → 59.0 m @ 07:22:31).
C4 tests: fake-mongo rows with MAVLink/ADS-B/null truth_match → exclusion, preference, joint distinctness, hysteresis; F1 replay 129→177 event emitted  — **DONE (C)** `tests/test_engine_selection.py` (14 tests, ~15 s, bare Python, no server): synthetic 4-track snapshot (MAVLink-matched target 20 m, ADS-B N432R 14 m NEAREST → excluded, null interceptor 30 m, decoy) → selection/exclusion/rules/events/meas_space; meta-absent tolerance; hard exclusion both roles; joint distinctness (single shared track, 3-track sum-minimal pair, radar claim beats proximity); other-role handicap; 1.5×gate; role_ids swap + pattern fallback; hysteresis band + priority override; interceptor hysteresis independent of target; 50-cap/newest-last/loss; bare-state + legacy-tuple conversion; F1 replay via `feed.archive_snapshot` 07:21:44–07:22:07 → target 129→177 @ 07:21:57 "position gate" + interceptor None→129 same tick (the steal). Pre-existing bare engine tests in test_apptest (`-k "engine or truth_handed"`) still pass. NOTE for D: `tests/test_review_fixes.py::test_meas_raw_obs_circles_...` fails on A3 (`circle-open` marker) and B's `OBS_COLS=8` (`archive_obs(1).shape[1] == 5`) — stale against A/B, not engine.

## D — integration (after A+B+C report) — STATUS: see "D — result" at the end of this file
D1 full test suite green; D2 single clean relaunch (setsid -f); D3 :8901/:8902 on LAN+Tailscale; D4 preview regenerated (1920/3796, pre-flight / pre-CPA / post-CPA + the 07:24:15 steal with the quad expanded, Data-source page in both modes + connected-to-fake, sidebar both modes); D5 tools bundle rsync + zip (credential grep clean); D6 update this file's status column; D7 design QA applied (below)

## Added later (owner A)
A15 layout: the three main figures (map, separation, error panel) fill ~85–90 % of the browser height (preset-derived panel height Laptop 600 / Desktop 860 / Large 1180 + runtime fine-tune from window.parent.innerHeight); MEASUREMENT SPACE quad is a collapsible section below (default collapsed, remembered in session state + `?meas=1`, iframe not rendered when collapsed, "m" toggles)
A16 layout: TOP-DOWN MAP is the PRIMARY display (≈60–65 % panel width 2-col, ~55 % 3-col, full height, track pills always visible); metrics are SUBPLOTS around it — right column: separation (top) + error cards (below; 2×2 grid if it reads better at reduced width); one-column mode: map first/tallest → separation → error cards; quad stays collapsible below
A17 fonts: global text-scale CSS var --ih-scale (sidebar "Text size" Normal/Large/X-Large; default Large on Large preset), all CSS in rem; base type raised (status .85 rem, tile values 2.0 rem, hero 3.2 rem, headers .85, captions .8; plotly fonts 13/14/16 px × scale; pills 13 px × scale); ui.font_px follows scale
B12 fonts: Data-source page + sidebar blocks in rem (body 1.05, headers 1.25, primary values 1.8–2.2 rem); presets now Laptop 600 / Desktop 860 / Large 1180 — align help text/tests
C5 meas_space: per-track on_tgt / on_itc boolean arrays (role-independent side tests) in addition to `on`

## D7 — formal design QA (owner D), from the `dataviz` and `design` skill rules — apply to final screenshots at 1366/1920/3796
dataviz: no dual axes; solid hairline grids only; legend iff ≥2 series (+ selective direct labels, never a number on every point); 2 px lines, ≥8 px markers with 2 px surface ring, ~10–30 % band fills; text never in a series colour (ink tokens only, colour carried by a mark beside it); status colours only for status and always with glyph+word; categorical hues fixed per entity (never re-assigned on filter/handover — a track keeps its slot colour for the session); sequential = one hue; run the palette check (python port in tests/test_palette.py) on ANY new colour; tooltips on every mark/series; hit targets ≥24 px in plots.
design: (1) AVOID the slop tropes — "rounded containers with a left-border accent colour" is called out explicitly → replace the 3 px LEFT status edge on chips/tiles with a status GLYPH+word row plus a subtle 2 px TOP rule or a filled status dot (keep square corners); no emoji/dingbat glyphs as icons → replace ● ▲ ✕ ○ ★ used as STATUS ICONS with small inline stroke SVGs (16/20 px grid, one style; the map ★ CPA marker in plotly may stay a symbol); (2) gap-based flex/grid layouts everywhere (no margin-spaced siblings); (3) hit targets ≥44 px for real controls (buttons, radios, selects on the Data-source page and sidebar); (4) type: 1–3 faces max (we have a sans + mono — keep), body ≥16 px at the default scale, sizes from one scale, fallbacks with close metrics; (5) tonal blacks/whites (our off-black/off-white tokens are fine), 0–2 accents (red accent + gold CPA — check chroma/lightness match in oklch), no gradient backgrounds; (6) generous negative space or controlled density — not both mixed on one screen; (7) copy: sentence case, no filler numbers/"data slop" (drop any tile/caption that isn't decision-useful); (8) check phone/laptop width behaviour is intentional (one-column mode). Record pass/fail per rule in the report; fix what is clearly a defect, flag judgement calls for the user.
D7 result (palette, official validator run 2026-09-10): entities target #e66767 / interceptor #3987e5 PASS (ΔE 19 protan); gold ★ and grey "other" are highlight/neutral (excluded from the categorical check by design); status good/warn/fail pass contrast, yellow↔green CVD warn → glyph+word mandatory (already). Meas-quad slot palette FAILED (magenta↔green 1.6, orange↔red 7.1, violet↔blue 1.9) → A18: colour = role, identity = lightness step + dash + pill: target ramp #f3a6a6/#e66767/#b5474a, interceptor ramp #8fb8f0/#3987e5/#1c5cab, 4th+ → grey; ramps pass --ordinal; cross-role pairs pass CVD/normal floors.

## Status — owner A (visualization), 2026-09-10 evening
| item | status | where |
|---|---|---|
| A1 per-sample gating (ON-TARGET full / DEPARTED thin .35 / STOLEN blue ramp; ★ first · ✕ last on-side sample; "→ departed" / "→ interceptor (stolen)" glyph) | DONE | ih/plots.py meas_fig, track_sides, _cat_xy (consumes engine on_tgt/on_itc) |
| A2 truth-envelope axes ±15 % (min ±0.6 km / ±20 m/s / ±2°) + ▲/▼ off-scale indicators with counts in hover | DONE | plots.truth_envelope, MEAS_MIN_HALF, annotations off_* |
| A3 obs: filled 6 px #cfd6de @.75 + surface ring, drawn first | DONE | MEAS_OBS_* |
| A4 ★/✕ id pills 13 px mono ink on CARD/RULE pill (12 px × text scale ≈ 13), legend/titles 12 px (× scale) | DONE | PILL_PX, MEAS_TITLE_PX, MEAS_LEGEND_PX, px() |
| A5 z-order obs → departed → interceptor-side → target-side → truth → markers/pills | DONE | meas_fig sections 1–4 |
| A6 error panel: coasts ≤ 3 s bridged (continuous lighter .6 line + σ-interpolated band), > 3 s = break, 4 px coast strip per card, caption | DONE | plots.merged_series, error_fig, LS.HEADERS["err"] |
| A7 both truths (red/blue 3 px), "#177 · target" / "#203 · interceptor", legend groups TRUTH/TRACKS/OBS, caption | DONE | meas_fig, LS.HEADERS["meas"] |
| A8 TRACK CHANGES card (More expander + ultrawide #more), tile "prev #129 · changed hh:mm:ss" + amber 10 s, "→ #177" ticks on err + meas, status "trk #177" | DONE | theme.events_rows/events_card_html, 1_live._tiles/_status_parts, plots.handover_marks, liveserver eventsCard |
| A9 bistatic range / rate (spa kernels; TX co-located → 2×mono note; obs bistatic fields) | DONE (engine.meas_space + bistatic_series were written by A before the split; C keeps them) | engine.bistatic_series, plots MEAS_TITLE/AXIS, geom_note |
| A10 one chip row ≤ 5 (feeds merged), separation 32 % (2-col) / 66 % (3-col) | DONE | 1_live._more_items, LS.SEP_FRAC / SEP_FRAC3 |
| A11 map track-number pills (red/blue 13 px, newest track point, ride the heads relayout, amber 10 s on id change) | DONE | plots.track_pills, LS push/response_body "pills", JS PILLS/mapAnnotations |
| A12 default frame = ENGAGEMENT BOX (archive passes ± 20 s + vehicles; live last 120 s; pad 25 %, min hw 500 m), fit_flight / fixed / follow, reset → default | DONE | plots.engagement_box, 1_live._frame/_view (reads frame_mode, legacy map_frame labels), JS mode "engage" |
| A13 aspect-correct ranges (1:1 px) on every programmatic range change, doubleClick:false → our reset, sat refetch debounced 800 ms, z18 for small boxes | DONE | JS aspectRange/plotPx/applyRange/satSoon, plots.aspect_range, track_correlation/live_correlator.py z=18 |
| A14 sidebar source line helper | DONE (helper) — B must call `T.source_line_html(mode, mru=, host=, run=, flight=)` from app.py | ih/theme.py |
| A15 panel fills the viewport (presets 600/860/1180 + JS viewportPanel from window.parent), meas quad collapsible (default collapsed, ?meas=1, "m" key, no iframe/figure when collapsed) | DONE | LS.PRESETS, JS viewportPanel/bindParentKeys, 1_live meas_toggle |
| A16 map dominates: SPLIT 62/38, SPLIT3 55/20/25 (error cards kept as a 4-row stack: 118 px each at Desktop — readable) | DONE | LS.SPLIT/SPLIT3 |
| A17 text scale: --ih-scale (Normal/Large/X-Large; default Large on Large-1440p), all CSS rem, base scale raised, ui.font_px = 13/14/16 × scale, readouts 14 / pills 13 / titles 12 × scale | DONE (B adds the sidebar radio key `text_scale`) | theme.text_scale/scale_css/setup, LS.layout(preset, text_scale), plots.px |
| Palette: colour = role, identity = lightness step + dash + pill (RAMP_TGT / RAMP_ITC / fold grey) | DONE | plots.RAMP_*, STEP_DASH, track_style; test in test_measurement_space_quad_truth_tracks_obs |

Owner-A tests: tests/test_apptest.py (viz parts incl. new test_meas_quad_gating_envelope_off_scale_and_steal, test_error_panel_bridges_short_coasts_and_breaks_only_on_dropouts, test_track_handover_event_card_tile_and_ticks, test_meas_quad_collapsed_by_default_and_toggle, test_text_size_scales_fonts_everywhere), tests/test_liveserver.py, tests/test_panel_js.py (+ aspect / double-click reset), tests/test_review_fixes.py, tests/test_palette.py.
Left to B: sidebar radios ("Map frame" options → Engagement / Fit whole flight / Fixed (custom half-width) or the frame_mode key; "Text size" radio key text_scale), the Spec-test deletions, test_paths display-matrix labels, test_apptest sidebar-labels test. Left to D: relaunch, preview (make_preview.py updated: meas at 1920 incl. the 07:24:15 steal), bundle.


## Status column — every item (owner D, 2026-09-10 evening, after integration)
| item | status | D note |
|---|---|---|
| A1–A13, A15–A17 | DONE (A) · verified by D in the suite + screenshots | A8 handover label on the ERROR PANEL moved into the card's top-right readout column ("→ #177 · 07:22:05", 3rd line) — it collided with the "now …" readout whenever the handover was recent (screenshot 1920 post-CPA / steal); the meas-quad label stays beside its tick |
| A14 | DONE (A helper) · B wired it (app.py sidebar source line) | icon+word now an inline SVG |
| A18 palette (colour = role, identity = lightness step + dash + pill) | DONE (A) · D re-validated | interceptor dark step #1c5cab → **#2a6cc7** (2.55:1 → 3.27:1 on the card; ordinal + cross-role checks pass); map pills: text in INK, role colour on the 2 px border (was red/blue text) |
| B1–B11 | DONE (B) | B11 stitched: Live "Save…" → `on_click=D.goto_save` (+ standalone fallback switch_page); crumbs "Ironhide · Data source · 1 / 2" / "Ironhide · Live · 2 / 2"; `P["role_ids"]` / `P["frame_mode"]` were already in the Live page's params; dead `.ih-spec` / `spec_tiles*` / `.ih-hero` / `hero*` removed from theme.py. **Coordinator (per user) 2026-09-10 pm:** Data-source ARCHIVE section lost its seek slider / play / restart / speed / jump-to-pass (controls with no data in view) → ONE primary "Load flight and open Live view" (`ds_open_live`); transport stays in the sidebar (archive mode) + Live page; jump-to-pass buttons currently NOWHERE (see open items) |
| B12 fonts | DONE (B) · D normalised to the one scale | Data-source page sizes snapped to .75/.85/1/1.15/1.3/2 rem |
| C1–C4 | DONE (C) · re-run green | `tests/test_engine_selection.py` (14) in the suite |
| C5 meas_space on_tgt / on_itc | DONE (C) · consumed by A1 | — |
| D1 | DONE | see counts below |
| D2 / D3 | DONE — the coordinator relaunched the current tree twice during integration (crash fix); D does the final single clean relaunch after the last served-code edit | :8901 + :8902 on 172.18.1.28 and 100.124.170.89 |
| D4 | DONE | :8899/ironhide_style_preview/index.html — 1920 × {pre-flight, pre-CPA, post-CPA}, 3796 × pre-CPA, 1920 steal (quad expanded, More open), running app: Data source LIVE (not connected) / ARCHIVE (?ds=archive) / CONNECTED to the fake unit, Live page archive + live modes (sidebar both modes) |
| D5 | DONE | bundle rsync (excl. __pycache__ / .pytest_cache / _preview_*), refactored quickdump.py in data/, README dashboard paragraph rewritten, credential grep clean, zip → VP_TrackAnalysis/mru91_track2895/ |
| D6 | DONE | this table |
| D7 | DONE (applied) | table below; kept by `tests/test_design_qa.py` |

### D — extra fixes during integration
- **Live crash (user, replay): `int(port_)` with port None** — Streamlit hot-reloaded `ih.liveserver` (module globals reset) while the Live page's `@st.cache_resource` kept the server → PANEL True / port None, and the page pushed into a STORE the handler thread never read. Fixed by the coordinator: `start()` attaches `srv.store` / `srv.images`, new `LS.adopt(srv)` re-binds `_SERVER` / `STORE` / `IMAGES`; `pages/1_live.py` derives PORT from `SERVER.server_address`; D added the same cache_resource + adopt boot to `app.py` and the regression test `test_module_reload_orphans_store_and_adopt_rebinds_port_and_store` (a NEW module object after `del sys.modules`, old handler thread serves a push from the new module).
- Deep link `?ds=archive|live` (Data-source mode selector by URL; used by the preview) — `D.apply_query_params`, tested in test_paths.
- Data-source LIVE INFO tiles: icon+word status rows (LIVE / STALE / LOST · TRACKING · DETECTING · ALIVE / STALE / NO FEED); sidebar last-data words aligned (LIVE / STALE / LOST).
- Ultrawide (3796) `#more` chips: value 1.6 × chip font + `overflow-wrap:anywhere` ("CONTESTED" was clipped, the feeds value wrapped 4 lines).
- Preview generator rewritten (`make_preview.py`): page chrome + real panel JS per state, wrapper shots of the running app; `_preview_fake_entry.py` (TEMPORARY, deleted after the run, never bundled) runs app.py with `tests/fake_mongo.py` pre-connected for the live-mode shots.

### D7 — result table (applied 2026-09-10 pm; each row is a machine check in tests/test_design_qa.py unless marked "visual")
| rule | result | where / evidence |
|---|---|---|
| (a) tone = 2 px TOP rule + icon+word row, square corners; no left-border accent | PASS | `.ih-tile/.ih-chip/.ih-events/.ih-callout`, panel `.ih-chip/.ih-events`, Data-source `.ds-tile`; `border-left` absent from every CSS block; mode-selector radius .5rem → 2px |
| (b) status glyphs = inline stroke SVGs (16 px grid, currentColor, one style), no dingbats in HTML | PASS | `theme.ICONS` (ok · amber · fail · na · red/live · gold/star · pause · reset); `T.glyph/T.dot`; panel JS `gw()` from the same icons; quad toggle uses Streamlit Material icons; plotly symbols (★ CPA, ✕ obs, ▲/▼ off-scale) stay symbols; header captions that NAME those symbols are legend text |
| (c) hit targets ≥ 44 px (buttons, radio options, toggles, selects, inputs, expander summaries) | PASS | `--hit:2.75rem` rules in theme.py + Data-source page (3 / 3.4 / 3.6 rem) — select-slider thumbs (Streamlit-native) are the one control not enlarged |
| (d) gap-based layouts, no margin-spaced siblings inside components | PASS | tiles / chips / events / callouts / kv / ds-tiles are flex columns with `gap`; `.gw` icon+word gap; wordmark gap |
| (e) one type scale, body ≥ 16 px, two faces | PASS | scale .75 / .85 / 1 / 1.15 / 1.3 / 1.6 / 2 / 3.2 rem; body / widget labels / kv / callout body / tables = 1rem (16 px at Normal); Inter + JetBrains Mono only |
| (f) palette — entity pair | PASS | validator: ΔE 19.2 protan · 29.0 normal · ≥ 3:1 (dark, surface #1a1d22) |
| (f) palette — target ramp `--ordinal` | PASS | monotone L, adjacent ΔL ≥ .06, hue spread 3°, light-end 3.19:1 |
| (f) palette — interceptor ramp `--ordinal` | PASS after fix | dark step #1c5cab (2.55:1, WARN) → #2a6cc7 (3.27:1); hue spread 2°; cross-pair vs #b5474a ΔE 19.7 protan / 26.5 normal |
| (f) palette — status trio | PASS (as status) | contrast ≥ 3:1; normal ΔE ≥ 19.9; yellow↔green CVD 7.2 = WARN band → mandatory icon+word (now enforced, SVG). The categorical "lightness band" check does not apply to status colours (validator scope note) |
| (f) track colours only from the ramps + grey | PASS | meas / map / error-panel track traces ∈ RAMP_TGT ∪ RAMP_ITC ∪ {fold grey, de-emphasis grey}; no series colour is a status colour |
| (g) legend iff ≥ 2 series | PASS | map (5) · sep (3) · meas (6, grouped) show; error panel with one graded track hides |
| (g) solid hairline grids, no dual axes | PASS | every axis `griddash solid · 1 px · GRID`; no `overlaying` |
| (g) text never in a series colour | PASS after fix | all annotations / text traces in ink tokens; map pills text INK + role border |
| (g) tooltips on every data series | PASS (judgement) | truth / tracks / separation / errors carry hover; decorations skip on purpose: leaders, blind rings, raw-obs background layer, σ bands (their line carries the hover), start/end markers (pills label them), legend proxies |
| design (5) tonal blacks/whites, ≤ 2 accents, no gradients | PASS (visual) | red identity mark + gold CPA; gold vs red accent ΔE 10.3 is flagged by the categorical validator but neither is a data series (identity mark / highlight) |
| design (6) density | PASS (visual) | one-screen panel: controlled density; Data-source page: generous |
| design (7) copy | PASS (visual) | sentence case labels; footer "★ / ✕" wording replaced by words |
| design (8) one-column behaviour | PASS (visual, from the earlier 1366 shots) | stacked scrolling column < 1150 px |

### D — result (2026-09-10 18:17)
**Tests: 139 / 139 green** (138 + the late setFrameHeight harness test) — 10 files: test_apptest 40 · test_paths 11 · test_live_mongo 25 (fake + MRU39 10.139.28.205 rows, reachable) · test_panel_js 9 (headless Firefox, incl. the viewport-fit regression) · test_liveserver 8 (+ reload/adopt regression) · test_review_fixes 6 · test_palette 7 · test_archive 8 · test_engine_selection 14 · **test_design_qa 7 (new, D7)**. Baseline before integration was 130 green; the 5 failures introduced by the D7 restyle (dingbat/border contracts, ★ in tile text, pill border, `md` typo) were fixed in the tests that encoded the superseded look. Last full fast-set run 105 green + slow set 33 green + smoke of the final Live-page edit (status run label) 5 green.
**Execution paths re-verified (AppTest matrix, tests/test_paths.py + test_live_mongo.py + test_archive.py):** archive replay F1–F4 at a pass · play / pause / restart / 1× 2× 4× (no clock jump) · flight select + "Load flight and open Live view" · Live↔Archive mode switching with the mode-aware sidebar and only-selected-mode page (fake + MRU39) · MRU-number connect (fake: auto-select newest run; MRU39: 275 runs listed newest-first) · role assignment → engine selection (radar match > ADS-B exclusion > position gate, joint, hysteresis; F1 129→177 event) · save-to-archive (tmp root) → flights.json → Archive dropdown → replays through the archive path · collapsible quad (default collapsed, ?meas=1, "m") · Text size (Normal/Large/X-Large → every font) · Map frame (engagement / follow / fit / fixed + reset) · deep links ?flight&t&play&screen&ds · no exception, no "None"/"nan" in tiles or status (LEAK regex on every row).
**Relaunch:** single clean `setsid -f nohup streamlit run app.py` (pid 607895) — http://172.18.1.28:8901/ 200 · http://100.124.170.89:8901/ 200 · :8902/health {"ok":true} on both after the first page load · /tmp/ironhide_dash.log clean (0 warnings / errors).
**Preview:** :8899/ironhide_style_preview/index.html (make_preview.py) — 1920 pre-flight / pre-CPA / post-CPA, 3796 pre-CPA (three columns), 1920 steal (quad expanded, More open), running app: Data source LIVE (not connected) · ARCHIVE · CONNECTED (fake unit) · Live page archive + live modes (sidebar both modes). Screenshots read; defects fixed from them: handover label vs readout collision, ultrawide chip clipping, raw run-collection name wrapping the LIVE status strip.
**Bundle:** Seawall_Ironhide_Testing/Seawall_Week_of_8-24/seawall_0824_data/Seawall_Aug2026_Tools/dashboard/ironhide_dashboard/ (rsync -a --delete, excl. __pycache__ / .pytest_cache / _preview_*), data/quickdump.py (thin CLI over ih.archive), README dashboard section rewritten; credential grep (unit-password / ssh-pass / auth-token patterns) clean; zip → VP_TrackAnalysis/mru91_track2895/Seawall_Aug2026_Tools.zip (2.1 MB).

### Open items / user calls (D)
1. **corr_lib.EnuFrame equirectangular scale bias (B finding):** 111 320 m/deg on BOTH axes → truth N scaled +0.36 % (E +0.1 %) at 33.75° N: 3–4 m at 1 km, 11 m at 3 km vs the radar's NED/pymap3d. Live path and saved archives share the frame (replay == live), but it is a systematic truth-vs-track range-scale bias — fix in corr_lib (outside the dashboard tree). USER CALL.
2. **Engine other-role handicap = 75 m (C1, `OTHER_ROLE_PENALTY_M = 0.5·GATE`):** a MAVLink match pointing at the OTHER role stays a position-gate candidate with +75 m; any null-match track up to 75 m farther wins. Tunable; USER CALL on the value.
3. **Jump-to-pass buttons are gone** (coordinator removed the Data-source transport block per the user); if wanted, they belong in the sidebar archive block or above the Live panel. USER CALL.
4. Interceptor ramp dark step changed #1c5cab → #2a6cc7 (contrast); map pills now ink text with a role-coloured border — both D7-driven, flag if the old look was intentional.
5. Tooltips: raw-obs background layer, σ bands, start/end markers, leaders and blind rings skip hover on purpose (unified hover stays readable); every data series has one. Judgement call.
6. `.ih-status` / sidebar "TRUTH ROLES TGT — · INT —" shows dashes until an explicit assignment (pattern-based roles render as "—"); consider showing the pattern-resolved ids.
7. Sidebar "LAST DATA · NO DATA YET" on the Data-source page in live mode: the sidebar reads `last_snap`, which only the Live page fills — cosmetic until the Live page has run once.
8. AppTest limitation (B): `switch_page()` runs a page standalone (no entry script / sidebar); tests keep the two-instance pattern (`_live_of`).

### D — late rebase (coordinator's direct edits, 2026-09-10 ~18:30)
- **plots.handover_marks → hover-only ▾ marks** (coordinator, user report: the stacked "→ #122 · 07:19:52" text covered data / readout): a small role-coloured ▾ at the top of each handover tick, hovertext = id + time; the TRACK CHANGES card stays the full list. D updated the three tests that pinned the text labels (test_apptest handover + meas-quad rows) and exempted the mark from the design-QA "text in ink" rule (it is a mark; the hover carries the words). The D7 table row "(g) text never in a series colour" stays PASS.
- **liveserver viewportPanel → setFrameHeight() + MutationObserver** (coordinator, user report: measurement-space strip mid-screen): Streamlit re-applies the preset iframe height on every rerun without remounting the byte-identical srcdoc, undoing the one-time fit. D added `tests/test_panel_js.py::test_viewport_fit_survives_streamlit_reapplying_the_preset_height` — an OUTER 1600×900 page with the panel iframe at 864 px: the inner JS fits it to innerHeight−top−20+4, the outer page re-applies 864 px twice (style + attribute), the observer snaps it back within a frame (Firefox harness).
- **OPEN (coordinator investigating): TOP-DOWN MAP "blinking in and out"** — likely the map react / satellite image path in the panel JS (`applyEnvelope` map branch: `f.layout.images = sat + heads`, `fitView`, `Plotly.react`). D did not touch the map JS; coordinate here before editing.
- Served code changed after D's 18:16 relaunch (plots.py / liveserver.py by the coordinator): Streamlit's module watcher hot-reloads them on the next rerun (the adopt() path keeps the panel server across that reload); a final clean relaunch is the coordinator's call.

## E — real-browser end-to-end fix pass (2026-09-10 evening, selenium 4.49 + headless Firefox 155 / geckodriver)
Harness: `tests/e2e_browser.py` (not collected by `pytest tests/`; run it explicitly, see its docstring; `IH_E2E_URL` targets a running instance, else
it starts its own side instance; screenshots + geckodriver console logs + JSON evidence in `tests/_e2e_shots/`). Panel state is reachable from
the browser through `window.IH` (read-mostly debug handle exposed by the panel core JS).

| complaint | root cause (reproduced in the browser) | fix | evidence |
|---|---|---|---|
| 2. "MEASUREMENT SPACE strip covering the view" | `setFrameHeight` resized only the iframe; Streamlit's `stElementContainer` is a **flex item with `flex: 0 0 <preset>px`** (+ an `!important` emotion height), so a panel fitted TALLER than the preset (1920×1400: 1012 vs 864 px) overflowed its wrapper and the quad toggle / More / footer overlaid the panel bottom by 132–151 px; fitted SHORTER, a dead band appeared instead | `frameChain()` walks iframe → element container; `setFrameHeight` pins `height`, `min-height` and `flex-basis` (`!important`) on every node, re-asserts through the MutationObserver (attributes on the whole chain) + a 1 s guard; `viewportPanel` measures the iframe's offset in the scroll container's CONTENT (`scrollerOf`) so scrolling the page can no longer inflate the panel | `test_panel_wrapper_never_overlapped` 1920×1600 / 3796×1600 / 1920×1080 / 1366×900 + resize: wrapper h == iframe h, toggle gap 15–16 px; `test_panel_js` outer-page regression (grow + shrink) |
| 1. "zoom out too far → snapping, edges don't load" | (a) `/figs.json` returned `unchanged` BEFORE computing a tile: any zoom/pan while PAUSED / frozen / on a stale run never got imagery (reproduced: 15 s after a paused zoom-out the tile still covered the old 2.5 km box); (b) the old `viewportPanel` used the iframe's VIEWPORT top: with the page scrolled, every Streamlit rerun grew the panel → the map's pixel aspect changed → `constrain="range"` re-derived the ranges (snap) and the tile no longer covered the new extent; (c) map axes `automargin=True`: wider tick labels far out changed the plot size → the equal-aspect constraint moved the view a few % on every react / reset; (d) fetch box only ±5 % + 100 m quantisation, sequential Esri GETs (≈ 3 s for 30 tiles) | (a) the `unchanged` branch computes `sat_for(view)` too; (b) scroll-safe `viewportPanel`; (c) fixed map margins (l 64 / b 44), automargin off; (d) `SAT_MARGIN` 15 %, keep-while-covered rule (`sat_covers`: the client's tile is kept while it covers the view at ≤ 2× its width — no refetch on small pans), a browser-side TILE STACK (older tiles stay under the newest until covered: no bare edge while the new one loads), `live_correlator` tiles fetched with 8 workers, cap 48; the map react is deferred while our 600 ms range animation is in flight | `test_map_view_lock_and_satellite_coverage`: 16 notches out (19.6 km), pan 18 km beyond the data, 30 notches in (280 m): ranges identical 3.5 s after each gesture while the replay ticks, tile covers the view in every leg, reset unlocks and re-applies the frame; `test_liveserver` sat tests (keep rule, paused tile) |
| 3. "track error covering the plots — the right side is t = now" | readouts / containment / title / CPA tag were annotations INSIDE the plot area, top-right (over the newest samples) | each error card = a 45 px HEADER STRIP above the plot area (title left, "now ±σ" + containment right, all `yanchor=bottom` at domain 1.0); the strip / gap geometry travels in `layout.meta.err` and the panel JS re-derives row domains, card rects and legend for ITS height (`errRows`; the server-built figure used to be squashed by the browser re-height); x automargin off + computed bottom margin (exact pixels); no x title (HH:MM:SS ticks, the separation card carries "time (PDT)"); CPA tag → hover-only ▾ at the top of the hairline; separation "CPA n m" label → the card header HUD (`hud.cpa` in the envelope) + hover; 2 % right padding on both time axes (`X_PAD_FRAC`; the separation card via an invisible `_anchor_pad`) | `test_readouts_outside_plot_area` at 07:22:31 / 07:22:45 / 07:23:52: no text annotation intersects any plot area nor the last 15 % of the time axis; padding ≥ 1.5 % |
| icons "suddenly much bigger" | head icons were layout images sized in DATA units (6 % of the frame width): the smaller engagement box and preserved zoom made them ≈ 3× larger and zoom-dependent | constant ON-SCREEN size: the panel JS sizes the images from the current m/px (`iconSizeM` = 44 px × text scale) in the tween loop, `placeHeads` and right after every user relayout; the 5 s velocity leader is now a client-side shape with an 18–90 px on-screen length (the map figure carries leader traces only on the st.plotly_chart fallback); server size = fallback only | `test_icon_size_constant_on_screen`: 43.5 / 43.5 / 43.2 px at the default frame, 3 notches in, 3 notches out |
| routing (found on the way) | a `pages/` directory next to the entry script puts a FRESH process into Streamlit's legacy page discovery: a direct `/live` URL ran `1_live.py` STANDALONE (no sidebar controls, no CSS, no deep link, default 300 px nav sidebar → the panel fell into stacked mode) until some session had loaded `/` | page scripts moved to `views/` (all references, tests and `st.switch_page` targets updated) | `Browser.open_live` asserts the styled precondition; reproduced 3/3 on the freshly relaunched :8901 before the rename, 0/N after |

Layout budget after the strips: `MIN_CARD_PX` 68 = the error card's PLOT AREA floor (under a 45 px strip = `plots.err_strip_px(14, 12)`, box model 1.35·font + 4 px measured in Firefox), `ERR_HDR_PX` 45, `ERR_GAP` 8, `ERR_MIN_PX` 512 (= `ONE_ERR_PX`); Desktop 860 keeps its natural sep 257 / err 547, a fitted 1080p panel (688) keeps two columns (sep 120 / err 512). The panel JS (`fitTwo`) and `liveserver._fit_two` share the rule; the header strip scales with the text size (X-Large: 56 px).

### E — verification record (2026-09-10 21:06 PDT)
- **Unit suite:** `pytest -q tests/` → **139 passed** (223 s; canonical order — `test_live_fallback_when_panel_port_unavailable` is order-sensitive to a cache_resource'd server from an earlier AppTest, unchanged).
- **e2e matrix (headless Firefox, side instance): 17 / 17** — wrapper 1920×1600 · 3796×1600 · 1920×1080 · 1366×900 (+ grow / shrink resize each) · map lock + satellite coverage (16 notches out ≈ 19.6 km, 18 km pan beyond the data, 30 notches in ≈ 280 m, reset) · readouts at 07:22:31 / 07:22:45 / 07:23:52 · icon size (43.5 / 43.5 / 43.2 px) · handover 129→177 (07:22:02) · steal (07:23:56) · controls matrix (Laptop / Large / Desktop; Normal / Large / X-Large; Follow / Fit / Fixed / Engagement + Reset; Freeze on/off; quad by button and by `m`) · replay 1× / 2× / 4× for 60 s (28 samples each: map mean 40–60 — no black / white frame; ONE satellite `<image>` identity set per run; stamp advanced on every sample; toggle gap 16 px; a 1500×900 → 1920×1080 resize mid-play at 1×) · LIVE mode against `tests/fake_mongo.py` (Connect → run followed → Live page ticks) + an unreachable custom host (error callout, no exception) · Data source → Load flight → Live → Play.
- **Extra defects fixed on the way:** the `m` hotkey died after any panel remount (the parent-document listener belonged to the unloaded iframe's realm → re-bound per mount, clicks the first VISIBLE toggle); a plotly-internal uncaught `_plots is undefined` when the tween's relayout interleaved with `applyGeometry`'s relayout/resize after a preset change (tween gated on in-flight fits + try/catch; plotly `responsive:false` — the panel resizes its figures itself); geckodriver appends its log, so the harness truncates it per browser session.
- **Relaunch:** interim (item 2 only) 20:06:06 PDT pid 814855; **final single clean relaunch 21:06:18 PDT pid 960778** — http://172.18.1.28:8901/ 200 · http://100.124.170.89:8901/ 200 · :8902/health `{"ok":true}` on both after the first page load · /tmp/ironhide_dash.log clean. Served smoke (`IH_E2E_URL=http://172.18.1.28:8901`): wrapper 1920×1080, icons, map lock/coverage, readouts 07:22:31 → green, log still clean.
- **Evidence:** `tests/_e2e_shots/` (129 PNG + JSON per scenario; `served_smoke/` for the :8901 run). Key shots: `wrapper_1920x1600_initial.png`, `wrapper_3796x1600_initial.png`, `map_view_far_out.png` / `_pan.png` / `_far_in.png` / `_reset.png`, `readouts_072231_panel.png`, `icons_icons_default.png` / `_in3.png` / `_out3.png`, `handover_129_177_panel.png`, `steal_panel.png`, `controls_*.png`, `replay_1x_sNN.png`, `live_fake_live_panel.png`, `live_fake_unreachable.png`, `load_flight_playing.png`.
- **Known / by design:** at 1920×1080 with Text size Large / X-Large the taller header strips + tiles push the fitted panel below the two-column budget → stacked (scrolling) mode, exactly like the Laptop preset; `probe_paused_paused_zoomout.png` (scratchpad) is the before-fix reproduction of "edges don't load" while paused.

## F — open-item closure (2026-09-11)

| item | decision / change | evidence |
|---|---|---|
| 1 corr_lib.EnuFrame scale bias | **FIXED in corr_lib** (outside the dashboard tree): `EnuFrame` is now the exact WGS-84 geodetic→ENU (geodetic→ECEF, tangent-plane rotation at the origin), scalar or array inputs, plus the exact inverse `lla()`; `mlat`/`mlon` are the WGS-84 curvature m/deg at the origin (110 917.8 / 92 656.6 at Seawall vs the old 111 320 / 92 561). Matches pymap3d to < 1 µm over 2000 points within 25 km. U is the tangent-plane up (curvature drop 1.3 m at 4 km — same convention as the radar's NED "D"). `engagement_tab.py`'s two private 111 320 copies now call the frame. **Finding:** the Seawall archives and the colleague dataset were written by `seawall_archiver` with pymap3d, so they were already exact — `track_correlation/fix_enu_columns.py --dry-run` over all 80 truth CSVs / 158 357 rows: max ΔE ΔN 0.01 m, ΔU 0.02 m, nothing rewritten. The bias lived only in the LIVE paths (ih.feed truth rows, ih.archive saves, live_correlator, post_correlate); replay ≠ live until now, now equal. Tests: `track_correlation/test_corr_lib_frame.py` (5), dashboard fake-mongo fixtures now synthesize track LLA with `frame.lla()`. | `test_corr_lib_frame.py`, `fix_enu_columns.py --dry-run` output |
| 2 engine other-role handicap | **Kept at `OTHER_ROLE_PENALTY_M = 0.5·GATE` (75 m)** as the default; no code change. Rationale: the official chaotic correlation has no exclusivity at all (greedy 1:1 in measurement space + 15 s plurality) and a hard exclusion would empty a role slot whenever both truths match the same track during a crossing; the soft penalty keeps a candidate while letting any unambiguous track win. Tunable constant; revisit with live MRU data. | `ih/engine.py:164` |
| 3 jump-to-pass buttons | **Added to the sidebar transport block (archive mode)**, under Replay speed: one button per VERIFIED pass, two per row, label `Pass n · HH:MM:SS · miss m`, tooltip with the truth-truth miss; `D.jump_to_pass(t)` seeks to `PASS_LEAD_S = 25 s` before the pass and plays (a seek is a new context: track-change log and CPA state cleared). Unverified passes (F3 passes 1–2, interceptor grounded; F3 pass 8, F4 pass 1) get no button. Never on the Data-source page (user rule). | `tests/test_paths.py::test_jump_to_pass_buttons_seek_lead_before_pass_and_play`, sidebar button-set assertions updated |

Not changed (still open, separate call): `corr_lib.GEOID_N = −31.4` vs the ulog-derived −29.4 (truth U ~2 m low); the dataset `run_info.json` records −31.4 so archives and live agree.

## G — velocity states on the main display (2026-09-11, user request; layout picked from the Claude Design canvas)

**Ask:** "see velocity filtered states as well for North East and Vertical … rewicker the layout … mock up with Claude design … swap the 3 panel
velocity with the 4 panel track position error … use SPA measurements, not re-inventing the wheel … adaptive works for 1920 / laptop … test the
full replay for bugs and visual issues before showing me".  Canvas: https://claude.ai/code/artifact/885b6e86-a722-4375-b67a-596e8e59d080
(page 1 = chosen Option A swapped + the adaptive fallback; page 2 = the earlier three-column-at-1920 sketch).

**Layout (adaptive, one code path; ih/liveserver.py `geometry(W)` + `layout()`):**
| panel width | mode | arrangement |
|---|---|---|
| ≥ 2400 px (user's 3796 display) | three | MAP (46 %) · SEPARATION (26 % of the middle column) over the three stacked VELOCITY cards (27 %) · the four ERROR cards (27 %). `SPLIT3 = (46, 27, 27)`, `SEP_FRAC3 = 0.26`; the More chips left the panel (they live in the More expander). |
| 1150–2400 px | two | MAP over a 3-across VELOCITY STRIP (left 62 %) · separation over the error cards (right). Strip = 24 % of the panel clamped 160..230 px (`vel_strip_px`): a fitted 1080p browser (~690 px panel) gives 165 px, the map keeps ~500 px; the block header is dropped in this mode (card strips + legend carry the names). |
| narrower / Laptop preset | one | stacked: map / separation / error cards / velocity cards (`ONE_VEL_PX` 391), the iframe scrolls. |
The velocity block (`#velblock`) is ONE DOM node the panel JS re-parents per mode (`placeVel`); the figure's three subplot rows are re-arranged client-side by `cardGrid(meta.vel, H, cols)` — stacked (row domains, one x tick row, axis titles) or side by side (column domains, each card its own 3 horizontal time ticks, no axis titles, the short readout `readout_<key>_s` shown instead of the long readout + Δ line).

**Figure (ih/plots.velocity_fig, key "vel", `UIREV["vel"]`):** per card (N · E · vertical, up positive): MAVLink truth velocity = thick solid target-red (measurement-space convention), the target track's FILTERED velocity state = ink line, same 2.5 px mark (never the truth's colour), lighter + coast strip for tentative / coasting samples, ±1σ band around the track line ONLY where spa had a real velocity σ (live block-143 covariance rows 3–5; the 8/28 archives were dumped without one → no band, "σ n/a"), gold CPA hairline + hover ▾, red now line, 2 % right pad, handover ticks. Header strip: title left · line 1 right "track −11.2 · truth −12.0 m/s" · line 2 right = **chaos-spa's grading**: "Δ +0.8 · σ 0.9 m/s · 1σ 67% · n 118" (Δ = spa `e_dot/n_dot/u_dot_errors` = track − truth at spa's interpolated truth time; σ = spa `*_dot_sigma`; containment = our |Δ| ≤ kσ rule over rows with a real σ). Empty rows keep an anchor point; legend iff truth + track both present.

**Data (ih/feed · ih/archive · ih/data · track_correlation/spa_errors · ih/engine — see the plumbing agent's report below):** track rows carry 16 columns (… vE, vN, vU, sigvE, sigvN, sigvU; NaN when the covariance is missing, never 0); archives write / read `sigvE_mps, sigvN_mps, sigvU_mps`; `build_tracks_raw` feeds spa the per-row velocity σ when present (else its 5 m/s fill, flagged so a fabricated σ never reaches a band or a rate); `errs_from_metrics` returns ve/vn/vu errors + sig_ve/sig_vn/sig_vu; engine `A["errors"]`, `A["errors_ung"]`, `A["err_tracks"]`, `A["contain"]` carry the keys.

**Tests:** unit — tests/test_liveserver.py (layout numbers: Desktop map 598 / vel 206, Large 894 / 230, strip clamp 165 @ 688), tests/test_panel_js.py (geometry per breakpoint incl. vel / velCols; the map tests derive expectations from the current, landscape range), tests/test_apptest.py + test_design_qa.py (figure key set incl. "vel", fallback chart order), plus the plumbing tests. Real browser — tests/e2e_browser.py `test_full_replay_flight_two_column[1..4]` (each flight END TO END at 4× in a 1920×1080 browser: all four figures drawn, envelope stamp advancing, no annotation inside any plot area or over the last 15 % of the time axis on sep / err / vel, velocity cards side by side, wrapper never overlapped, map never a flash frame, screenshot every 60 replay-s) and `test_full_replay_flight1_ultrawide` (3796×1879, three-column, block in the middle column).

**Findings surfaced by the new cards (flight 1):** the target orbited a ~90 m circle before its run-in (the truth N/E velocity sinusoid 07:20:45–07:21:45, confirmed by position derivatives); track #177's altitude state is pinned at 100 m from 07:22:06 while its filtered vertical velocity drifts to −76 m/s (−111 m/s on #203 by 07:25:53) — a tracker pathology the position cards hide.

## H — "error plots need to pop when there is an issue" (2026-09-11, after the from-scratch design round)

User verdict on the design round (canvas https://claude.ai/code/artifact/1a17d2f7-fce7-492f-b785-d5379c5a053f, six boards on two pages): keep the existing UI; only the error cards must signal trouble.  Change (ih/plots.py, `card_tone` / `card_shape_style`): every error card and velocity card now carries a STATE — **ok** = the quiet card as before (rule border 1 px); **warn** = amber 2 px frame + words in the title line ("▲ OUTSIDE 1σ", "▲ 1σ RATE 54%", "▲ Δ 9 m/s" when no σ); **fail** = amber 3 px frame + warm card fill (`CARD_WARM`) for |err| > 3σ, a 1σ rate < 30 %, or an unphysical velocity Δ (> 20 m/s).  Rules: the colour lives on the card frame/fill (state), never on text or the series (target red stays the vehicle's hue — the reviewers' "target-red doubles as alarm-red" critique); the words are primary ink; over-confidence (rate > 80 %) stays quiet; a missing σ never raises a tone by itself.  Verified on F1: 07:21:50 el/3D/alt fail (altitude state 278 m vs 76 m truth), 07:22:31 quiet except vU unphysical, 07:23:52 (post-steal) velocities fail.  Satellite underlay and replay unchanged (the design round never touched the served code).  Test `test_live_figures_follow_mark_and_chrome_spec` accepts the three tone styles.

## I — clarity round (2026-09-11 late): no flashing, no jitter, clearer labels, sectioned cards, zoom-out imagery

User: "the flashing colors don't help · not sure what the vertical blue lines are · labels and current errors clearer · section the plots off · render the satellite larger so you can zoom out · high priority: no jittering, everything easy to see".
- **State frames off** (`PL.TONE_ENABLED = False`): the amber warn/fail frames flipped every second as the error crossed σ → read as flashing. Code kept for a hysteresis version if ever wanted.
- **Blue ticks** = interceptor handover marks drawn on the TARGET's error / velocity cards → `handover_marks(..., roles=("target",))` on those panels (the measurement quad still marks both roles).
- **Labels / readouts**: card title 12 → 14 px, "now ±σ" readout 14 → 17 px, second line 12 → 13 px (header strip 45 → 51 px; card plot-area floor 68 → 60 so Desktop 1080p keeps map 598 / sep 257 / err 547 and a fitted 1080p browser stays two-column: `_fit_two(688) = (122, 510)`).
- **Sectioning**: card frame `CARD_RULE #4a5160` at 1.5 px (was the page rule #30353d at 1 px), gap 8 → 10 px.
- **No jitter**: `PL.stable_range(mem, fig, key, lo, hi)` — a per-session memory (`s["_yrng_mem"]`, cleared on seek) holds every card's y-range; a wider need widens at once, a narrower need shrinks only after staying < 60 % of the held span for 20 s. Applied to the error cards and the velocity cards.
- **Satellite**: `SAT_MARGIN` 0.15 → 0.5 (the fetched tile spans 2× the view each way, so a 2× zoom-out still has imagery; the tile zoom level drops automatically under the 48-tile budget); tile corners now placed through the exact WGS-84 frame (`EnuFrame.lla`) instead of 111 320 m/deg.
Tests: tests/test_clarity_2026_09_11.py (frames off + section style, stable_range widen/hold/shrink, target-only ticks, tile box 2×); pinned literals in test_liveserver / test_apptest now derive from the constants.

## J — clipping audit + operator asks (2026-09-11 evening)

**Clipping audit** (`tests/clip_audit.py`, Selenium + Firefox — Playwright is not installed here; the harness has the same geometry access): Live page + Data source page at 1920×1080 / 3796×1879 / 1366×768 × Text size Normal / Large / X-Large. Reports text overflow, elements clipped by an overflow-hidden ancestor (horizontal and vertical judged separately — a scrollable axis is not clipping), elements past the viewport, plotly annotations outside their card / figure, tick labels outside the figure, overlapping annotations, header overflow. Findings and fixes:
- sidebar horizontal radio rows (Screen size, Text size, Map frame) overflowed the 360 px sidebar at Large / X-Large → `.stRadio [role=radiogroup]{flex-wrap:wrap}`;
- the "Map zoom — centre to edge (m)" label + help icon overflowed at X-Large → label is "Map zoom (m)";
- map track-number pills of two close tracks overlapped (#177 / #200) → `depill()` in the panel JS stacks colliding pills a row apart (screen-pixel test via mpp);
- two velocity-card tick labels sat 2 px outside the figure → within the 3 px tolerance (cosmetic).
After the fixes: panel-side issues 0 at every size / text scale.
**Operator asks:** tiles reordered — TARGET TRACK (state · coasting) first, separation, closest-so-far last; "Save data to archive…" is a full-width primary button on the Live page and in the sidebar LIVE block (was a small "Save…"); Data source page simplified — mode selector text 1.9 rem, plain captions, roles + save inside expanders (roles open only while a role is empty; save opens from the Save shortcut), the panel-port card folded into "Connection details".

## K — round 3 (2026-09-11 evening): titles, lines, truth, scaling — two agents + verification

User (verbatim, several messages): "you didn't increase any titles for the plots, still very hard to make out", "what are these random vertical lines", "make the truth state more obvious for the velocity plots", "better axis scaling … don't need ±20 angle error", "why is this still tiny", "spin up multiple agents … make sure you don't miss anything before re-serving".
- **Plots (ih/plots.py):** card title 20 px bold PRIMARY ink (was 14 px secondary), readout 20 px, second line 15 px → header strip 57 px. Vertical elements inventoried: red "now" line REMOVED (the right edge is now; 2 % pad kept); gold CPA hairline kept and LABELLED on the first card ("CPA 59 m · 07:22:31"); target handover ticks kept and LABELLED on the first card ("→ #177 · 07:21:57", anchored left of the tick near the right edge, stacked when < 20 % apart); x grid solid hairline only. Velocity cards: truth 3 px target-red with an end label "truth", track 2.5 px ink with "track". Axis scaling: half = nice(max(floor, 2·median σ, 1.25·P95(|err|+1σ))) clamped az/el ±5°, alt ±150 m, 3D 0–300 m; velocities span ±15 % floor ±2 clamped ±30 m/s (an unphysical −80 m/s pins at the edge, readout still shows it); stable_range hold kept between the two clamps.
- **Chrome (ih/liveserver.py, ih/theme.py, app.py):** panel header 28 → 36 px, header title primary ink at max(18, font+4) px; card plot-area floor 60 → 53 so a fitted 1080p browser stays two-column (`_fit_two(688) = (110, 506)`); presets Desktop map 582 / sep 252 / err 536, Large 878 / 355 / 753; status line 1.15 rem; Text size defaults: Desktop → Large, Large 1440p → X-Large.
- **Tests:** tests/test_plots_round3.py (15), tests/test_chrome_round3.py (6); AppTest sessions seed Text size Normal; assertions moved to constants; design-QA rule now REQUIRES labelled lines on the error/velocity cards (the ▾ hover marks remain on the measurement quad). Full suite 164 passed (MRU39 probes deselected: unit down). Browser: clip audit + full four-flight replay before the relaunch.
- **Answered without code:** "double buffer everything" — the panel already draws in place (Plotly.react, no teardown), stacks satellite tiles old-under-new, tweens icons client-side; Streamlit's own top line / tiles re-render per rerun by design. "GPU acceleration" — WebGL (scattergl) was the cause of the map blinking on every react (context re-creation) and was removed deliberately; the browser composites the SVG on the GPU already.

## L — 2026-09-14: MRU connect row, quiet panel server, MRU39 idle
- User: "the MRU text size is still tiny". The Data source connect row is now in a keyed container (`ds_connect`): label 1.6 rem primary ink, the MRU number field 4 rem tall with a 2 rem mono numeral, the resolved address line 1.6 rem, Connect 4.4 rem tall at 2 rem; other page inputs 1.3 rem.
- Weekend log: nine `ConnectionResetError` tracebacks from browsers (10.30.1.x) dropping a `/figs.json` socket mid-response — harmless. The panel server (`_QuietServer`) now swallows connection-reset / broken-pipe / aborted / timeout on a request thread and logs anything else in one line.
- MRU39 is reachable again (4 of 5 probes pass); `test_mru39_run_with_tracks_renders_grey_ungraded` now SKIPS when the newest run with TRACKS has none inside the live window (unit idle) instead of failing.
- Browser: the remaining e2e group (wrapper, map lock, icons, smooth replay, controls, handover/steal, fake-Mongo live, load-flight) rerun on the final tree before the relaunch.
- Verification on the final tree (2026-09-14 17:30–17:45 PDT): browser e2e `test_handover_and_steal_render` ×2 + `test_live_mode_fake_and_unreachable` pass (the measurement-quad start label now flips below the title when the first sample sits in the top 30 % of the range; the custom-host field is clicked before typing and committed with a real Enter key). `tests/test_apptest.py` 40/40. Served :8901 (started 17:27 PDT, current source) smoke: error-card ranges held for 40 s of 4× replay (0 changes), velocity 8, no interceptor ticks, section frames #4a5160 1.5 px, satellite still covers after a 2× zoom-out, no console errors; the Data source connect row measures label 25.6 px / numeral 32 px in a 64 px field / Connect 70 px tall.
- Test-harness note: two pytest runs of this repo must never overlap — they share the test panel port 8912 (`IH_LIVE_PORT`), and the loser's `LS.start` returns None for 30 s (`RETRY_S`), so every AppTest in that window falls back to `st.plotly_chart` and `figs_of` raises `KeyError: <sid>` (23 spurious failures seen when a suite from the previous session was still running).
- Final state 2026-09-14 17:27 PDT: unit suite 168 passed / 1 skipped (MRU39 idle); :8901 relaunched (pid 1664630), LAN + Tailscale 200, :8902 health ok on both, log clean. Served Data source page in a 1920×1080 Firefox (Desktop preset → Large text): MRU label 29 px, numeral 37 px in a 74 px field, Connect 37 px text / 81 px tall full width; DOM overflow audit of the page 0 issues.

## M — 2026-09-14 evening: smaller live-view type, one label per event, nothing over the plots
User (with a 2560-wide screenshot): "the text is very massive on the live view even on Normal", "drop CHAOS-SPA GRADED from everything", "make it so you can see the plot and the numbers don't overlap them", "this view locked covers everything", "I don't need track number labels and CPA on EVERY plot".
- **Type:** card title / readout / second line 20 / 20 / 15 → **14 / 14 / 11 px** (× text scale); header strip 57 → **46 px** (`err_strip_px(14, 11) == (46, 21)`, a 2 px `STRIP_ROW_GAP` between the rows — the two boxes overlapped by 2 px in Firefox); panel header row 36 → **32 px**, header title/status `--hf = max(14, figure font)` (was max(18, font + 4)). ERR_MIN 506 → 462, ONE_VEL 386 → 353; a 1080p browser still fits `_fit_two(688) = (162, 462)`.
- **Vertical marks:** the target handover tick is on the **azimuth card only**; its label(s) are one list in that card's header strip, second row left ("→ #129 · 07:21:50   → #177 · 07:21:57", newest 3, annotation `tick_labels`) — never in the plot area. Velocity cards carry no vertical marks. The measurement quad keeps handover ticks / ▾ and the "#id" start–end pills on the **range panel only**. The CPA gold ★ + hairline stay on the **map and the separation card only** (the separation header already reads "CPA 59 m · hh:mm:ss").
- **View-lock chip** ("view locked · ⟲ reset") moved from an absolute box over the map into the map header's right group, header-sized.
- **Wording:** "Track quality" header (no "· chaos-spa graded"), velocity caption "Δ/σ/containment vs truth", footer "GOLD STAR = CPA ON THE MAP AND SEPARATION ONCE GATED · TRACK QUALITY GRADED ON CONFIRMED+UPDATED STATES", build badge without the suffix.
- Verified in Firefox at 2560×1440 (Normal and X-Large): no strip annotation overlaps another or intrudes into a plot area (err + vel), pill sits in the header row (top 2 px, map plot starts at 32 px).
- Also: the Plotly modebar is OFF on the separation / error / velocity figures (it sat over the first card's readout; the map keeps it); the map header caption is re-fitted whenever the tween readout or the view-lock chip changes (clip audit: 34 px header overflow at 1366 px).
- Verification: unit suite 168 passed / 1 skipped; browser e2e 16 passed (handover + steal, unreachable host, flight-1 replays at 2560×1440 two-column, 1920×1080 two-column — fits again —, 1366×768 stacked, 3796×1879 three-column, wrapper / map lock / icons / smooth / controls / load-flight); clip audit panel issues 0 at 1920×1080, 3796×1879 and 1366×768 × Normal / Large / X-Large after the caption fix. Relaunched :8901 2026-09-14 18:47 PDT (pid 1809140), again 19:04 PDT after §N (pid 1841143).

## N — 2026-09-14 night: the three banner tiles leave the Live page
User: "why are these boxes so smushed, make those 3 top panels much smaller — they aren't very useful", then "target track is the only real useful thing, but we also show that on the plot". The TARGET TRACK / SEPARATION / CLOSEST-SO-FAR tiles no longer render above the panel: the target track's state joins the top status line ("trk #177 ✓ CONF", amber "TRACK CHANGED" for 10 s after an id change; "trk — · NO TRACK" otherwise), and the three tiles render inside the "More details" expander below the panel (same markup, unchanged tile tests). The panel starts ~50 px higher at 2560×1440.

## O — 2026-09-14 late: "everything jumps / flashes on every update" (laptop), status-line icons, "graded"
- **Measured first** (real headless Firefox, served page, 20 s of 1× replay at 1366×768 and 1920×1080; `scratchpad/jitter_probe.py`): no iframe reload, no Plotly range change, no layout-shift entries inside the panel — but the panel iframe's top moved 32–36 px every few seconds. Cause: the status strip shares a flex-wrap row with the wordmark/crumb/badge; every tick its text changes (PAUSED, state word, TRACK CHANGED) and the row wrapped / unwrapped, moving everything below it. Second finding: Streamlit dims a running fragment's elements (`stale-element`, opacity .33 after 0.5 s) — on a slow laptop that is a once-a-second flash. Third: the per-frame tween relayout carried `annotations` (pills) although pills only change per server tick; benchmark (`relayout_bench.py`) images 0.3 ms + shapes 0.3 ms vs annotations 6.2 ms per call at 12–15 Hz.
- **Fixes:** `.ih-top` is now two rows with FIXED heights (row1 wordmark · crumb · badge, 1.7 rem; the status strip 2 lines + 2 px under 1800 px viewport width, 1 line above; content may wrap inside, the box never grows) — the panel below can no longer move. `[data-testid="stMain"] .stale-element { opacity:1; transition:none }`. The tween relayout is images + shapes only; pills are drawn by placeHeads() once per tick / view change; tween rate floors at 4 Hz above 40 ms. The map's "RADAR" text label is gone (it clipped at the plot edge whenever the origin sat just outside the view; the hollow ring stays). The "tween 15 Hz · 10 ms" header readout is diagnostics only (`SHOW_TW`). The status strip shows NO vehicle picture icons any more; MAVLink feed trouble appears as words only (TGT FEED STALE 12 s); the empty "TGT — · INT —" roles label is suppressed. "graded" removed from every user-visible string (status "172 samples", readouts "no sample(s)", hovers "· tentative" / "· coasting", footer "TRACK ERRORS FROM CONFIRMED+UPDATED STATES", Data source "grey with no truth match"); the solid-line trace names end in "matched".
- **Serving:** the served instance now runs with `--server.fileWatcherType none` — Streamlit re-imported every saved source file on the next tick, so edits (and half-edits) went live while the user watched. Edits now reach the user only through the relaunch.
- **Tests:** design-QA / chrome CSS guards exempt the one deliberate fixed-height block (`.ih-top`); e2e wrapper test's short-viewport rule uses viewportPanel's own 420 px floor (the old `top > 200` escape only held because the tall wrapped top bar pushed the panel down); AppTest top-bar regex for the two-row structure; trace/readout wording.

## O — 2026-09-14/15 night: "everything jumps / flashes" measured and fixed; laptop first
User: "everything (plots, map, images) is jumping on every update/flashing — a robust fix so we stop going around in circles", "the picture icons in the status line are useless", "why do you still say graded", "laptop display is highest priority", runs Chrome.
- **Measured first** (scratchpad/jitter_probe.py: in-page 100 ms iframe-rect sampler, MutationObserver, Plotly call wrappers, PIL pixel diffs, headless Firefox against the served page at 1920×1080 and 1366×768): no iframe reloads, no map range changes, error y-ranges held; the panel iframe moved 32–36 px every few seconds because the top status bar wrapped/unwrapped as its text changed (PAUSED, TRACK CHANGED, roles) — the ONE real "jump"; the map ran ~12 Plotly.relayout/s for the tweened icons + pills (≈250 per 20 s) — the "laggy / flashing" on a laptop.
- **Fixed-height status row** (theme.py `.ih-top`: row1 wordmark + crumb + build badge 1.7 rem; `.ih-status` = `--st-rows` wrapped lines, 2 under 2400 px / 1 above, overflow hidden, sized so a glyph-carrying line fits) + Streamlit stale-element dimming override (opacity 1, no transition). The empty "TGT — · INT —" roles part is gone; MAVLink feed health is WORDS only when not alive ("TGT FEED STALE 12 s"); no vehicle icons in the status line. Sidebar transport / jump-to-pass buttons one per row (labels wrapped to 2–3 lines at 1366).
- **Map DOM overlay** (liveserver.py `ovMake/ovDraw`, `IH.overlay`): vehicle icons (one animated SVG per role rotated by CSS transform, `will-change`), 5 s leaders, live segments and track pills are DOM elements positioned from `gd._fullLayout` l2p every frame (30 Hz, ~0.1 ms) and clipped to the plot area; Plotly gets satellite tiles only. Map relayouts per 20 s: 242 → **0** (reacts unchanged ≈ 10). Follows drag / wheel / our animate; icon edge constant on screen (51 px at Large). The 2026-09-10 "teleport" cannot recur: the browser owns the view (fitView keeps the current range), so the overlay projects through the same range Plotly draws with; tests/e2e_overlay.py asserts heads within bounds of the trail end every second through reacts, a 200 px drag and two wheel notches.
- **Wording:** "graded" removed everywhere user-visible ("75 samples", "no samples yet", hover "· tentative / · coasting", trace names "… matched", footer "TRACK ERRORS FROM CONFIRMED+UPDATED STATES", Data source "grey with no truth match").
- **Figures (plots.py):** map key = one row in a font-derived top margin under a reserved modebar row (never over imagery, never wraps down), `map_margin(font, scale)` → 83/12/62/56 at default (automargin off, so the equal-aspect view never snaps); interceptor radar track left the key (pill + caption identify it); pills target above-right / interceptor below-right; err/vel strips line up with the drawn cards (top margin = strip + legend row, no autoexpand drift); y ticks thinned on short cards; quad "(stolen)" tag flips inside. clip_audit panel findings 85 → 2 over 1366×768 / 1536×864 / 1920×1080 / 2560×1440 × Normal / Large / X-Large (new checks: ticks-collide, axis-title-over-ticks, legend-over-plot-area, legend-under-modebar). tests/test_stacked_laptop_layout.py (41 static tests).
- **Regression tests:** `test_page_never_moves[1366-768|1536-864|1920-1080|2560-1440]` (40 s, 100 ms sampler: iframe top/height, .ih-top/.ih-status heights, scrollY, iframe loads, figure div heights, opacity; Pause→Play inside the TRACK CHANGED window) and `test_status_row_fits_fixed_box` (× Normal/Large/X-Large: scrollHeight ≤ clientHeight, every span inside). With the fix disabled at 1366×768 the sampler catches a 37.6 px jump. Firefox has no layout-shift API (the geometric sampler is the detector).
- **Ops:** the served instance runs with `--server.fileWatcherType none` (edits no longer go live on the next rerun) and is served from a SNAPSHOT dir `../ironhide_dashboard_served` (rsync of the verified tree, same ARCHIVE_ROOT parent) so agents can keep editing the working tree. Relaunched from the snapshot 2026-09-15 07:03 PDT (pid 2873365): unit 209 passed / 1 skipped, browser 8 key tests green; served laptop probe: iframe top constant over 30 s, 0 reloads, 0 map relayouts, ≤ 0.5 % of panel pixels change per 0.5 s (was 15 % on jump frames).
- In flight (agents): track counter in the status line (confirmed / tentative / coasting updates + Hz for the target track over the metrics window); panel follow-ups (plotPx margins, text-scaled stacked card heights, sep header clock overflow, quad modebar off); laptop audit; headless-Chrome verification (the user runs Chrome); measurement-quad declutter at laptop widths.

## P — 2026-09-15 morning: laptop-first live iteration (served from snapshots, user testing between pushes)
Served instance: `../ironhide_dashboard_served` (snapshot; `--server.fileWatcherType none`); hand edits copied file-by-file + one relaunch each.
- **Laptop panel (liveserver):** mode "one" is TWO-UP — square map card (plot area square, e.g. 413×413 at 1366×768) beside the separation card, error + velocity cards full width below; figure widths pinned to their divs on every geometry pass (+ ResizeObserver) — no dead width after a resize / sidebar collapse; icon edge = min(44×scale, 10 % of the map plot height); map/sep keys off in one-mode (margin.t reclaimed); follow re-frame at most every 10 s unless a head leaves the view.
- **Card headers:** ONE row wherever the plot-area width allows (title left · readout right · containment only if it fits · CPA/handover label only if it fits), else title on the top line and readout on the second — never overlapping (panel JS `stripOneLine`, fit computed from the browser font); shaded header band (`hdr_<key>` shapes) on every card; readouts 18 px / titles 16 px / sub 12 px (strip 52); units in titles ("AZIMUTH ERROR (°)", "VERTICAL VELOCITY (m/s)"); velocity side-by-side cards = same two-line layout, numbers line "−81.6 · +0.0 · Δ −81.0 m/s" sized to the card, no bold; margins l/b/r of the side-by-side and separation cards derived from the BROWSER font (clipped tick labels both sides before).
- **Vertical marks:** gold CPA hairline back on all 4 error + 3 velocity cards (`cpa_marks`), labelled once in the azimuth strip; separation card lost its "time (PDT)" axis title (it sat on the ticks with the fixed bottom margin).
- **Status row:** ONE row, 1.3 rem, no icons, no "1×", no "F1" (sidebar says it), full words: "07:22:49 · #177 CONFIRMED · 127 CONFIRMED · 3 TENTATIVE · 3 COASTING · 2.0 HZ · PAUSED(last)", counts colour-coded green / amber / red; engine state words spelled out (CONFIRMED / TENTATIVE / COASTING). Map top row carries the track status tag ("#177 CONFIRMED", state colour).
- **Regressions fixed in-flight:** `\00b7` CSS separator rendered "�B7" (Python escape) → literal "·"; missing `_sw` helper (NameError on the Live page for ~1 min); satellite pan self-heal listened to its own relayout event → 12 Hz redraw loop ("back to jittery") → user range edits only; PAUSED slot placeholder dropped (PAUSED appended last).
- **MRU91 "unreachable":** probe took ~2.3 s vs a 3 s timeout → `PROBE_TIMEOUT_MS = 8000` for probe/connect (per-tick fetches keep 3 s); message "did not answer within 8 s".
- **Defaults:** screen preset Laptop (Normal text) for new sessions. Chrome for Testing 153 verified the anti-jump fix (tests/e2e_chrome.py).
- Measured on the served page (1366×768, 20 s, 1×): iframe rect changes 1 (initial), loads 0, map relayouts 1 (a heal after a follow animate), figure reacts ≈ 1 Hz; ≤ 1.3 % of panel pixels change per 0.5 s.
- Open: unit tests to re-pin for the hand edits (run in progress); 1920×1080 X-Large separation-card findings (panel agent note); status row clips ~20 px at 1366 × X-Large.
- Suite re-pinned to the round-P behaviour (three agents, tests only): **309 passed / 1 skipped** (MRU39 idle skip). Real bugs found by the re-pin and fixed in the working tree (NOT yet served — the user was testing live): follow cooldown started at every check instead of at a re-frame; the compact velocity line showed "Δ —" when spa had no graded Δ (now track − truth like the stacked line); the panel restores the middle ("0") y tick label when the one-row header makes the drawn row ≥ 3 labels tall. Pending: one relaunch from the tree when the user is done testing.

## Q — 2026-09-15 late morning: LIVE against MRU91 (Jade_Sheep) actually works
Symptoms (user, live test): Live tab blank 30-60 s then "no data / NetworkTimeout", "NO TRUTH" while the Data source listed a MAVLink feed, tab switches ~1 min, MRU91 "unreachable", port field showing 1.
- **Fetch path (feed.py):** MRU91's link is slow (143 doc 75 kB, 106 doc 48 kB with ~122 ADS-B sub-payloads); the old snapshot pulled the whole 120 s window per tick → 20-35 s → timeout every tick. Now: first tick = newest 12 s, then forward page + ONE 12 s back-fill chunk per tick within a 1.5 s budget (`backfill_s` exposed), per-block-type try/except, truth via an aggregation that `$filter`s payloads to MAVLINK (240 kB → 3.8 kB per chunk), NED-only 143 projection, batch_size 20 / max_time_ms 6000, one shared MongoClient, per-run constants fetched once, stale-hold 30 s from the buffer. Measured: tick 0 1.7 s with truth + tracks, then 0.1-0.3 s (was 20 s + timeout).
- **Page (views/app):** first fetch only inside the fragment; main-script runs re-serve the last snapshot; Live chrome paints in 0.47 s (was 6.65 s), tab switches 0.15-0.4 s (was 0.8-1.1 s + blocking), zero Mongo calls on navigation (live_info/AR.preview were running on every Data-source rerun); status "CONNECTING…" → data → "LAST DATA n AGO" (stale) → OFFLINE only after 30 s; cadence = max(refresh, last tick + 0.5 s) shown in the sidebar; AppTest inline-fetch hook (`IH_FETCH_INLINE`).
- **Truth roles (engine/plots):** interceptor-only truth draws (trail + head + its radar track), status/tag "NO TARGET FEED" (amber) vs "NO TRUTH FEED" (red), card readouts "no target truth feed", sep readout says why; unassigned MAVLink ids (matching neither pattern) draw GREY with an id pill ("mav14552_3_0 · unassigned") — `feed_role()` never merges a third drone into the interceptor any more; `snap["other"]`, `A["other_truth"]`, `A["n_unassigned"]`.
- **Map hover:** altitude (m HAE) + speed on every truth trail/head and radar track (truth U is metres above the radar → antenna HAE added for both).
- **Feed cleaner (data.py):** keeps the first AND last row of a parked position, so a drone sitting on the pad shows its icon (was: first row only → aged out → no icon).
- **Data source:** Advanced fields (port / db / custom host) bound through widget keys — the collapsed expander dropped the widget-backed keys and the port fell to 1 ("Connection refused"); probe timeout 8 s, "NO REPLY … did not answer within 8 s"; live socket timeout 8 s.
- Also: save button moved to the bottom of the Live page; sidebar key/value block wraps at spaces only.
- Served: full tree rsync → ../ironhide_dashboard_served, relaunched 11:15 PDT; headless check after Connect (MRU 91) → Live: status row "LIVE JADE_SHEEP · TGT FEED NONE · INT FEED FROZEN 0 s", interceptor head visible on the map at the pad, no exceptions.
- Open: sidebar "LAST DATA" still reads NO DATA YET on the Live page (sidebar renders before the fragment; the age is in the status row); refresh default stays 1 s (cadence auto-stretches).
