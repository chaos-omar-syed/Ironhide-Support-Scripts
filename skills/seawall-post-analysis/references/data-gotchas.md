# Data gotchas — conditioning & interpretation catalogue

Apply every applicable item BEFORE trusting a number. Each entry: symptom → detect → do → where already handled → where seen.
Sources: Seawall week 2026-08-25..28 (MRU91, runs 707ccda9 / 9b22a989 / 214a1a56 / e65bd4f9), MRU39/MRU43 follow-ups, and the
`track_correlation/` toolkit (`corr_lib.py`, `eng_plots.py`, `engagement_tab.py`, `tracking_tab.py`, `toxic_zones.py`, `postprocess_report.py`).
Archive: `Seawall_Ironhide_Testing/Seawall_Week_of_8-24/seawall_0824_data/` (README.md = column/units contract; per-day `flights.json`
`method`/`issues` blocks = machine-readable version of much of this file).

---

## A. Units & frames

| # | Item | Symptom if ignored | Detect | Do | Handled in | Seen |
|---|------|-------------------|--------|----|------------|------|
| A1 | **Block 106 AIR_TRAFFIC `altitude` = FEET, MSL** (ADS-B schema; MAVLink source too) | Truth altitude 3.28× too high → phantom el error ~4°/~200 m, fake "−583 m truth datum", "+5° el bias inside 1.5 km" | Parked drone reads U ≈ +400–500 m; el error scales with range | `alt_HAE_m = alt_ft*0.3048 + GEOID_N` then geodetic2enu | `corr_lib.mavlink_alt_hae_m`, `load_truth(alt_units='feet')`, `_guard_alt_units` (warns on units flip; never mutates); archiver writes `alt_ft_wire` + `U_m_hae` | 8/26 `fast_extract.py` bug (report regenerated). Do NOT re-litigate. |
| A2 | **`vertical_speed` = FEET/MINUTE** (not cm/s — earlier guess wrong) | vU off by ×197 or ×0.6 | raw median ~69, max ~1854 = textbook ft/min drone climb (0.35 / 9.4 m/s) | ×0.3048/60 = 0.00508 | `corr_lib.VS_FTMIN_TO_MPS`, `vu_scale()` (P95<50 → already m/s, passthrough) | 8/26 |
| A3 | **`speed` = KNOTS; `speed_mps`, `velocity_n_mps`, `velocity_e_mps` = m/s** | Speed gates wrong by 0.51× | — | Use `speed_mps`/vel_n/vel_e; never the wire `speed` | archiver drops wire `speed`; `toxic_zones.py` derives speed from vel_n/vel_e when `speed_mps` missing (quickdump CSVs lack it → coverage denominator silently zeroes) | 8/31 week rollup |
| A4 | **Block 143 `x_state` = NED `[N,E,D,vN,vE,vD]`, metres, WGS-84 HAE, about antenna origin** | Swapped axes give km-scale errors | Compare vs truth: only E=x[1], N=x[0], U=−x[2], vE=x[4], vN=x[3], vU=−x[5] gives 35–52 m | Use that mapping; σ from `p_cov` diag P[1,1],P[0,0],P[2,2] | `corr_lib.load_tracks` (13-col t,E,N,U,sigE,sigN,sigU,lu,assoc,state,vE,vN,vU); archiver README | Verified `px4_vs_track.py` 9/11 |
| A5 | **Obs (103) and track (143) share the SAME altitude reference** = RX antenna-group centroid HAE (`antenna_location_origin_*`), orientation 0 → NED, el = geometric above local horizontal | "obs vs track datum" arguments | Single-target dwell: obs `el_rad` ≡ track `el_angle_rad` to 10 sig figs | Nothing to correct | — | 8/27 (do not re-litigate) |
| A6 | **Antenna origin is PER-DEPLOYMENT (per run)** | 1–2 m U offsets, wrong ENU | Read `antenna_location_origin_*` from a track / `meta.json` | Never hardcode. 8/25 707ccda9 141.76 m · 8/26 9b22a989 139.882 m · 8/27 214a1a56 140.908 m · 8/28 e65bd4f9 140.51 m HAE (lat 33.7480x, lon −115.3392x) | archiver `meta.json`; `_rr_satmap` fallback chain — quickdump meta key is `"antenna"`, archiver key is `"antenna_origin_lat_lon_haeM"` | 8/27 vs 8/26 |
| A7 | **Geoid N** (HAE − MSL) | 2 m constant U offset | MAVLink − PX4 `altitude_ellipsoid_m` = +29.4 on every log | Site N = **−29.4 m** (PX4-derived). Archiver/`corr_lib.GEOID_N` use −31.4 → archived `U_m_hae` is 2.0 m LOW. Harmonise to −29.4 when rebuilding; state which was used | `corr_lib.GEOID_N`, `tracking_tab.init_mission(geoid_n=)` | `alt_compare.py` 9/10 |
| A8 | **`corr_lib.EnuFrame` was flat-earth (111 320 m/deg both axes) until 2026-09-11** → +0.36 % N, −0.10 % E (14.5 m N at 4 km) | Systematic range-scale bias truth-vs-track on LIVE paths only | — | Archives were written with pymap3d (exact, verified `fix_enu_columns.py --dry-run` max 1 cm). Live paths before 9/11 (live_correlator, post_correlate, ih.feed saves) were biased. EnuFrame now exact WGS-84 with `lla()` inverse | `corr_lib.EnuFrame` | 9/10–9/11 |
| A9 | **2026.9.x block-143 layout (version_id 1.7)**: NO `x_state`/`p_cov`; has `x_state_ecef`, `p_cov_ecef`, `latitude_rad/longitude_rad/altitude_m`, `track_velocity_n/_e`, `range_m`, `range_rate_mps` (opening-positive) | `corr_lib.load_tracks` returns `{}` SILENTLY | Empty tracks dict on a run with block 143 docs | Position via track LLA through EnuFrame; σ = sqrt(diag(R·P_ecef·Rᵀ)) | `ironhide_dashboard/ih/feed.py track_rows` handles both layouts; `ih/archive.py` | MRU39 9/10 |
| A10 | **Obs `amb_dop_ms` positive = CLOSING**; opening-positive range rate = −amb_dop_ms; spa uses −2·amb_dop bistatic | Sign-flipped closing speed | — | Note sign per source | `spa build_obs_df` | MRU39 |
| A11 | **`2026-08-27/from_cache/*.csv`** `vU_mps`/`vert_spd_mps` ALREADY m/s (converted at cache time), unlike live dumps' `vert_spd_wire_ftmin` | Double conversion | Check column name | Read README column contract per source | archiver README | 8/27 |
| A12 | **Time bases**: MAVLink feed stamps (radar receive time) run ~0.5–1 s LATE vs radar clock; published track `t_epoch` ~0.9 s AFTER `last_update_t`; cached detection stamps 0.5 s before tracker update | Track appears to lead PX4 by 0.6–1.9 s; few-m error | Track-vs-feed shift −0.2…−0.7 s | Cite feed latency when aligning; effect on error metrics ≤ few m | — | `px4_vs_track` 9/11 |
| A13 | Track state velocity (cols 10–12) is the FILTER velocity; **truth velocity = reported vel_n/vel_e, NEVER differentiated positions** | Noisy fake velocity error | — | `corr_lib.truth_vel_at` (nearest sample within 3 s) | `report_figs.posvel_fig` | 8/26 |

---

## B. Truth-feed defects

| # | Defect | Symptom | Detection rule | Fix | Handled in | Seen |
|---|--------|---------|----------------|-----|------------|------|
| B1 | **Stale 1 Hz republish** — legacy `capture_mavlink.get_message()` falls back to mavutil's message cache; last GLOBAL_POSITION_INT republished forever with fresh `time.time()` stamps. Only `docker restart chaos_main_network_capture_truth` clears it (≥2026.4.1b0 has `MAVLINK_TRUTH_STALE_AFTER_SEC`) | Truth "continuous" but frozen; interpolation garbage; coverage scored vs a parked point; live "telemetry dropouts" that were really hovers | Exact-position repeats (<1 cm step) or runs of ≥3 near-identical positions (eps 1 m). Pcap-proven: zero wire traffic while rows kept landing. 8/25: 92–95 % of rows stale; 8/27 interceptor frozen 08:34:24→09:46:25 (4 321 s); 8/28 `2_34` frozen 1 154 s, `2_1` 652 s; target froze between every 8/28 flight | Strip before anything else: `corr_lib.clean_truth(min_move_m=0.01)`; `eng_plots.drop_frozen(eps=1, minrun=3)` (drop whole run); 8/28 manifest method: per-feed frozen runs ≥3 identical dropped, first kept | `clean_truth`, `drop_frozen`, `condition()`; **NOT** handled by `toxic_zones.TruthInterp` (keeps repeats → 8/27 09:51:50–09:52:38 freeze scored as coverage against a frozen point — known defect) | every day |
| B2 | **Frozen target truth inside an engagement** | No truth for the whole engagement (8/27 eng2: 14550 froze 09:57:45 → end, 3 719 s at the 1 880 m home point) | Position change = 0 for minutes while the other craft manoeuvres | (a) onboard ulog GPS (Fox log_62 gave eng2 CPA 39 m); (b) radar-only CPA recovery (D2); (c) live: `live_correlator` "tgt track" manual override (forced track drawn dotted) | `engagement_tab.load_traj_csv_truth` for ulog truth | 8/27 eng2 |
| B3 | **GPS teleports / EKF runaways** | Fabricated crossings (36/121/19 m "passes"), blown-up plot extents | Implied speed to BOTH neighbours > vmax (single-sample teleport); step > max(200 m, 40 m/s·dt); toxic gate 250 m/s with dt<30 s; truth >20 km from antenna = glitch. 8/27 14551: 523 m/s teleport, 1 122 m jump at unfreeze, 331/170/121 m burst at dive bottom. 8/28 target EKF runaway after F2 landing: phantom ground-level run to E 4.5 km (28–45 m/s at U −14 m) then 2 835 m teleport back to pad + 5.7 min freeze | Drop the sample; rolling-median on REAL samples; break lines at gaps; **gate by airborne** (U > ground+5 m or speed > 2 m/s) so ground-level runaways never score | `eng_plots.declutter(vmax=130)`, `clean_truth`, `toxic_zones.TELEPORT_MPS/RANGE_GATE_M`, `eng_plots.action_box` robust percentiles | 8/27, 8/28 |
| B4 | **Duplicate MAVLink ids for one vehicle with complementary gaps** (multi-component: `mav14551_2_0/_2_1/_2_34`) | Each feed alone looks gappy; samples at the CPA missing | Same sysid, different compid, interleaved timestamps | MERGE by rounded timestamp (0.5 s), dedupe, then clean. Do it per drone: target = `mav*_1_*`, interceptor = `mav*_2_*` | `live_correlator --interceptor` engagement mode; `engagement_tab.load_dump_truth`; 8/28 manifest method | 8/27 (3 ids), 8/28 |
| B5 | **Alias revert on redeploy** — `/chaotic_vol/config.json` mavlink aliases wiped by redeploy; ids flip `mav14550_1_1` ↔ `mavlink_1_2`; after `capture_truth` restart ids become port-tagged (`mav14550_1_2`) | Truth "disappears" (id changed) or two ids for one flight | Id name changes mid-run; `runs` collection stores the config snapshot per run (compare `deployed_by/hostname`) | Edit config.json (dir not writable — back up to /tmp) + `docker restart chaos_main_network_capture_truth`; RE-VERIFY after any redeploy/run-cycle (8/28 fix at 06:44 could be wiped by a concurrent redeploy). Un-aliased period rows on 8/25 were all stale placeholders — no usable truth before the fix | archiver keeps one CSV per id; analyst concatenates | 8/25 11:30, 8/26 13:36→14:01 UTC, 8/28 06:44 |
| B6 | **Interpolating across dropouts fabricates crossings** | False CPAs, chords across the map | Gap between consecutive real samples | NEVER interpolate across > 3 s (live metrics / INDEX rule) or > 2.5 s (8/28 pass method); `eng_plots.cpa(max_gap=2)` evaluates at REAL interceptor samples; `condition(resample_dt, max_gap=4)` gap-aware; `toxic_zones.TRUTH_GAP_S=5`, `TRUTH_NEAR_S=3`; `truth_vel_at` 3 s | `cpa`, `condition`, `closing` (gap-broken), `_gap_line`, `TruthInterp.query` | 8/27 eng1 |
| B7 | **Hover below MDV** — hard empirical notch: zero obs below \|dop\| 3.2 m/s (8/25, 3 081 obs; p1 4.1), 3.7 m/s on 8/26. Hover = NO obs formed, not detected-then-discarded | Takeoff/landing/hover "gaps" blamed on radar | truth speed < ~3 m/s | Exclude from coverage denominators (`toxic_zones.MOVING_MPS=2.5`, `corr_lib.MOVING_MPS=2.0`, 8/28 method >2 m/s); `gate_metric` matches over MOVING samples only (clutter can shadow a parked drone but not follow it); `clamp_agl(20 m)` kills pad junk | `coverage_grid`, `gate_metric`, `clamp_agl`, `track_deaths` (gap capped at last MOVING truth) | 8/25 drone-1 hovers; 8/26 both ends |
| B8 | **Official chaotic truth validity** (`air_traffic_targets_matching_utils`): needs `heading_valid` + `validposition`, rejects `alt==0` and `speed==0` (`filter_zero_speed`), ≤30 s stale (`ADSB_AGE_LIMIT_SEC`) | A hovering drone is NEVER officially truth-matched; `payload.truth_match` empty | — | Don't read absence of `truth_match` as "no drone"; archive `validposition` (rows with 0 kept for record; `TruthInterp` drops them) | `toxic_zones.TruthInterp` | MRU39/MRU91 9/10 |
| B9 | **Fake truth from a test sender** | A "target" with identical hand-entered position every doc, speed 0, heading 90, 4-decimal lat/lon, timestamp advancing | Constant position + zero displacement over hours | Ignore / kill sender AND restart capture_truth (B1) | — | 8/25 `mavlink_1_2` 16:38–18:52 UTC |
| B10 | **Truth altitude "datum bias"** (+400–500 m parked, −583 m) | Vertical error with a huge constant | — | Was A1 (feet). Never subtract a "datum": use `err_stack` (RAW, rigorous HAE), not `corr_lib.angle_errors` (subtracts median dU — legacy) | `tracking_tab.corrected/ang_raw` (DU=0) | 8/25–8/26 |
| B11 | **1 Hz sampling under-reads fast crossings ~2×** (40 m/s closing × 1 s ≈ 40 m) and feed latency transients (F3 alt σ 9 m in the fastest dive: holds then jumps −68/+40 m) | CPA too large; alt spikes near CPA | — | Refine each pass with `E.cpa` in ±10 s (8/28 F3 44→21–23 m); treat dive alt spikes as latency, not datum | `build_overview_section` | 8/28 |
| B12 | **Which bird is which** | Wrong roles | Position xcorr of ulog vs each feed id | 8/27–28: 14550 = target (Fox), 14551 = interceptor (Zeus, sys_id 1); confirmed by trajectory xcorr (2.5–6.4 m). 8/25: `mav14551_2_2` was the FLYING drone (all tracks), 14550 mostly hovered | flights.json per day | 8/25, 8/27 |

---

## C. Track defects

| # | Defect | Symptom | Detection | Do | Handled in | Seen |
|---|--------|---------|-----------|----|------------|------|
| C1 | **Coasting** — publish rate constant ~2 Hz regardless; `last_update_time` unchanged between docs = coast | Coverage over-counted (live 96 % vs offline 91.4 % on 8/28); "measurement rate 2 Hz" when real is 0.4–1.9 Hz | `diff(last_update_t) == 0` | Fresh gate: `\|t − last_update_t\| ≤ 1.5 s` (coverage), 1.2 s (`toxic_zones.COAST_GAP_S`), ACTIVE = confirmed + updated within ±0.75 s (`COVER_WIN_S`). Measurement rate = count of lu changes | `corr_lib.match_track` (fresh col 14), `meas_times`, `toxic_zones` classify | 8/25 (0.27–0.47 Hz), 8/28 |
| C2 | **Death-coast runaway** — after the last measurement the track coasts on a bad velocity before deletion (U to −600…−3000 m at −49 m/s) | Error tails, fake "drift" at track end | Samples after the final lu change | Truncate matched samples at the FINAL measurement; mask coast excursions > max_horiz | `corr_lib.prep_matched`, `tracking_tab.prep` | 8/26–8/27 |
| C3 | **Altitude pinned at 100.0 m + vU nonsense** — 2026.3.4post15 `altitude_bounds.min_alt_meters: 100.0` (`MacTracker.apply_altitude_limits`, antenna-relative floor every update+birth); x[5] free → runaway to ±200 m/s; coasting integrates it | Track U = 100.0 for essentially every straight-leg track (truth 75–90 m); σU 60–140 m; junk −1 022/−798/−288 m on 8/27 eng2 tracks; vU spikes −77 m/s; el "bias" +0.37–0.5° ≡ +20 m TRACK-only | `track_vu_usable()` false; U == 100.0 exactly on 58–74 % of confirmed samples inside 2 km | **Never use track U/vU on 2026.3.x data**; altitude/vertical rate from MAVLink/PX4. Replay proves floor alone causes it (floor off → U 73.6 m, vU within −3…+2). Fixed geodetic in ≥2026.5.1b0 but x[5] unchanged; MRU39 2026.9.x still has min_alt 100 (7–12 % samples) | `corr_lib.track_vu_usable`, `eng_plots.condition(du_max=60 m/s, u_pad=300 m)`, `traj_mine ALT_BLOWUP_M=100` trim, `report_figs.posvel_fig` (vU only if `vu_ok`) | all days; `replay_mac_0827.py` |
| C4 | **Delta 200 m/s speed filter kills tracks** — `track_output_track_sender_ipc._process_tracks` drops tracks with `track_speed_mps` = √(vN²+vE²+vD²) (INCLUDES diverged vD) > 200 (or alt > 6 096 m) BEFORE publish; hard-on, no config key | Long-lived track "dies" mid straight leg with detections continuous; target untracked 9–86 s | Track speed at last published sample 198–200 m/s (week max 199.7, 0 above 200). 9 drone tracks cut: 8/26 94@08:41:00→32 s, 501, 917→86 s, 1226, 1566→71 s; 8/27 298, 454; 8/28 **950@08:03:25→80 s (= the "78 s radar-side hole")**, 1205 | Check last-sample speed before blaming association/LOS/range. Current builds cap 130 m/s (MRU39 speeds cap at exactly 130) | — (analysis-side check only) | REVISES `track-drop-causes` 9/11 |
| C5 | **Fragmentation / handovers** — 12 ids per flight on 8/26; turn handovers at truth heading rate > 8°/s; mid-leg association drops WITH detections continuous ~2/s (SNR ~50 dB) → association failures, not detection | Coverage gaps 10–50 s; wrong "successor" named | Rider chains via 150 m median gate; ≥5 states within 150 m | Name successors from data (8/26 trk 367's successor at the far NE turn 08:46:41 is **501, NOT 391** — verified). Turns cost 1.8× coasting (8/26 1.7×, 8/27 1.9×; 8/28 INVERTS 0.8× because holes sat on the straight eastern leg — narrate as such). New tentative tracks form on the drone mid-gap | `toxic_zones.load_target_tracks`, `track_deaths`; `_rr_turn_zoom_fig`; `traj_mine` handover stitching (≤3 s) | 8/26 |
| C6 | **Tentative vs confirmed** — `track_state` 1 = tentative, 2 = confirmed | Grading tentative states inflates errors/coverage | state column | spa grades ONLY confirmed + updated (adopt); ACTIVE gate = confirmed; late target track 1124 on 8/27 was "confirmed-but-degraded" (59 m/57 m 1σ) | `spa_errors.py`, `toxic_zones` | 8/27 |
| C7 | **ADS-B exclusion is POSITIONAL only**; `is_adsb_flagged`: classifier prediction 'UAV' ⇔ NO ADS-B match | "UAV" label mistaken for a drone id; far ADS-B traffic "stealing" a track | — | Classification does NOT isolate a drone — find it by kinematics (persistence, AGL, speed). ADS-B truth prunes far traffic; its altitude not load-bearing. Official `truth_match` with ADS-B callsign → HARD EXCLUDE; MAVLink id → target track; null → 150 m gate | `corr_lib.load_adsb_truth`, `auto_correlate`, `is_adsb_flagged` (comment "unpopulated on 2026.3.x" is likely wrong — plurality matcher landed 2026-03-06) | 8/25, MRU39 |
| C8 | **Side assignment** | Wrong steal/coverage bookkeeping | — | Target-side = median horiz < 150 m to target AND ≤ median to interceptor (≥3 truth-valid samples); interceptor-side symmetric; JOINT assignment — never the same track for both (official correlation has no exclusivity; strict exclusion empties a role slot during crossings → engine 75 m other-role penalty) | 8/28 method `track_sides`; `ih` engine | 8/28 |
| C9 | **Early-life altitude junk** while `total_associations` < 10 (trk 1102 dU +82…+323 m) | Fake dive/climb at track birth | assoc count | Ignore U before ~10 associations | — | 8/27 eng2 |
| C10 | **Tangential velocity under-read** (vN corr 0.5–0.68, slope 0.5–0.7; radial vE ~1.0) | Velocity-error plots look directional | — | Quote radial/tangential separately | `px4_vs_track` | 9/11 |
| C11 | **Range σ under-confident** (F1 #177: 1σ containment 3.3 %, median σ 22.8 m vs \|err\| to 133 m) | Containment stats look broken | — | Report containment; don't trust σ_range for gating | dashboard error cards | 8/28 F1 |
| C12 | **Obs multipath/sidelobe leak** at same az+range, different el (~+8°, same SNR) | Smeared obs el plots | — | ±3° el gate when matching obs to truth (`tracking_tab.obs_errors`); raw el is a COMPRESSION not a constant bias: measured_el ≈ 1.34° + 0.19·true_el (8/27 legs 1.5–4 km), height error +15 m/km; el estimator mode-switching trimodal (+0.09/−0.66/+0.57°) at equal SNR | `tracking_tab.obs_errors` | 8/26, 8/27 |
| C13 | **Blind zones / clutter wall** — min detectable range = c·pw/2 (5 µs → 0.75 km; search 30 µs → 4.5 km) | "closest detection 0.74 km" claims | — | Never use min(all obs range) as closest detection (it's the clutter wall); 8/28 truth never < ~1 km → blind zones did not constrain | `blindzone_map.py` | 8/28 |
| C14 | **Tracker idle / jobs failing** | 0 % coverage attributed to detection | First fresh track update of the day (8/25: 14:39:50, 3 min into the two-drone window); block 136 JOB_FAILED (E3) | Score only within the tracker-active window; state it | flights.json `tracker_first_fresh_update_pdt` | 8/25 |

---

## D. Engagement pitfalls

| # | Pitfall | Symptom | Rule / method | Handled in | Seen |
|---|---------|---------|---------------|------------|------|
| D1 | **False CPAs** from (a) interpolation across gaps, (b) pad clusters / parked or taxiing craft (6–32 m "passes" while parked; 8/28 F3 550/509 m pre-liftoff, F4 531 m while taxiing iU −14.6), (c) slow formation segments, (d) live frozen-sample artifacts (F3 351 m not reproducible offline), (e) sub-prominence dips (F2 428 → 437 m prom 60 m; F3 84 m prom 59 m) | Inflated pass counts, "best pass" on the ground | Pass = local minimum of 3-D separation **< 75 m**, ≥ 20 s apart (keep smallest), gated **REALISTIC: both craft ≥ 3 m/s AND closing ≥ 8 m/s into the minimum**; then **refine each via `E.cpa` in ±10 s** (1 Hz under-reads ~2×). Pass LISTING (manifest): 1 Hz grid, minima < 600 m with **80 m prominence per contiguous segment**, no interp across > 2.5 s. Mark quicklook-only passes with `*` | `engagement_tab.build_overview_section` (lines ~101–121), `eng_plots.cpa` (REAL interceptor samples, truth interp ≤ 2 s) | 8/27, 8/28 |
| D2 | **Radar-only CPA recovery when truth fails** | No truth at the pass | Two drones close HEAD-ON: filter track PAIRS on closing speed + opposite heading (velocity cos < −0.3), constrained to realistic drone speed **5–45 m/s** (rejects noise tracks at 100–175 m/s), anchor one member to whatever partial truth exists. Rejects parallel interceptor SPLITS (cos ~+0.9) and clutter (fast pair 1140+1133 at 35 m was 3 km off truth). Result is provisional: 8/27 eng2 195/187 m → superseded by ulog GPS-GPS **39 m** | (method in 8/27 memory; `interc_eng1_recovered.csv`) | 8/27 eng2 |
| D3 | **Steal — formal rule** | Track keeps its id but follows the other craft | Target-side track (median < 150 m, **≥ 5 pre-pass on-target samples**) whose **≤ 120 s post-pass median flips to the interceptor side within 120 m**. 8/28: trk **177** stolen at F1 pass 3 07:23:47 (post 46 m to interceptor vs 931 m to target); trk **1551** born ON the interceptor 08:39:05, swapped to target at F4 pass 6 08:40:04; trk **129** dragged (111 m) then died 07:22:04. TERMINOLOGY: every one of these is a **"track steal"** (no "capture"/"reverse capture") → 8/28 = 3 track steals | 8/28 method `steals`; `_rr_steal_gif`, `_rr_duel_fig` | 8/28 |
| D4 | **Corruption ≠ steal** | Error blows up at a close pass but identity holds | Corruption = identity HOLDS, state runs away (8/27 run 214a1a56 trk **633** @ 09:51:28; eng1 track alt runaway +280 m; eng2 post-CPA blow-up; 8/28 F4 repeat). Steal = identity flips. Show corruption with the ±12 s `err_window_fig` | `postprocess_report` §3 "Track corruption at close passes" | 8/27, 8/28 |
| D5 | **"The concern is identity, not coverage"** | Coverage % looks fine while the fight ate the track table | Post-crossing target-track error stays in the normal 20–70 m band with ~zero untracked seconds (another track always reacquires) → red flags are identity events only (F1p1 129, F1p3 177, F4p6 1551). Error-at-pass ≈ miss magnitude (F4 32 m passes: track err 59–64 m EXCEEDS the miss) → radar-only endgame scoring is ±1 pass-width | `stakeholder_0828.py` concerns_rollup | 8/28 |
| D6 | **Late-born target track** | No track sample in the CPA window (8/27 trk 1124 born 10:28:32, +32 s after dive bottom) | Search **±120 s** around the CPA for the closest track sample (fallback), keep the display window at t−10..t+5 | `engagement_tab.build_engagement_tab` line ~231 | 8/27 eng2 (187 m) |
| D7 | **Dive bottom** | Anchor point misfires on raw gradient | `dive_bottom()` needs the resample-to-1 Hz version (steepest descent then first vz > −2 m/s); NOT the later slow-sink trough | `eng_plots.dive_bottom`, `dive_time` | 8/27 eng2 (10:28:00) |
| D8 | **Stray feed-hole orphans stretch CPA-window plots** (8/27 eng1 points at t−17 s → axis −7..+1) | Guidance panels with a lone dot far left | Trim to the contiguous cluster containing t0 | `eng_plots.main_cluster`, `last_run(gap=2.5)` | 8/27 |
| D9 | **Guidance-only metrics** (user framing) | Fly-through/coast numbers quoted as guidance performance | Dash = contiguous `nav_state == OFFBOARD` (GCS cmd 176; terminal body-rate targets 23 Hz); report in-dash CPAs only; radar overstates endgame CPA +20..27 m vs truth (eng1 79 vs track 106; eng2 64 vs 84) | `dash_story.py` | 8/27 |
| D10 | **Interceptor crossings cost coverage** — interceptor within 200 m = 25 % of red cells; interceptor-side churn 4–5 tracks per 4-min fight | Post-fight holes (8/28 F2 79 s) | Attribute to the fight, not geography | `drop_factors` | 8/28 |
| D11 | **Onboard GPS supersedes feed** for CPAs when available (eng1 87 → 70 m; eng2 195 → 39 m); feed "teleports" were feed-only artifacts (ulog clean) | Contradicting numbers across reports | State the source per number (truth-truth / ulog / vs-track) as in the 8/27 DAY_SUMMARY passes table | flights.json `passes[].source` | 8/27 |
| D12 | **CPAs are truth-3D** (unaffected by C3); interceptor crossed 29–47 m ABOVE target on 3/4 8/28 passes — consistent with guidance consuming the high track altitude but **UNVERIFIED** | Over-claiming cause | Label as unverified | — | 8/28 |

---

## E. Mongo access traps

| # | Trap | Symptom | Fix | Where |
|---|------|---------|-----|-------|
| E1 | **`time_spec_float` is NOT indexed** (indexes: `_id`, `(block_type, job_id)`, `(block_type, time_spec.full_sec DESC, time_spec.frac_sec DESC)`) | `find({block_type:106, time_spec_float:{$gte}})` scans every 56 kB ADS-B doc → >60 s → 3 s socketTimeout on every tick | Filter on `time_spec.full_sec` (int floor/ceil), re-apply the float bound in Python; count with the same key. Newest doc: `find_one({}, sort=[("$natural",-1)])`; newest of a type: sort on `time_spec.full_sec/frac_sec`. **NEVER `sort $natural -1` WITH a filter**. Block-106 truth: add `"payload.source":"MAVLINK"` + projection (~40 ms) | MRU39 9/10; `ih/archive.py` chunked indexed queries |
| E2 | **Payload is a LIST (or dict) bundling MANY targets per 106 doc** | `payload.$` positional projection silently drops all but the first match (14551 looked like 7 docs at ~1 Hz) | Iterate the full payload list; project subfields only (~5× faster) | `corr_lib.load_truth`, archiver |
| E3 | **Block map**: 106 AIR_TRAFFIC (truth, ADS-B + MAVLink, only while a Run ID is active) · 143 TRACKS (job_id 0, timestamp-indexed) · 103 DWELL_WITH_OBS (obs bundled; standalone 101 absent) · 145 hits · **136 JOB_EVENT lifecycle** (JOB_REQUESTED→…→JOB_COMPLETE_SP / JOB_FAILED + `event_error_type`) · 135 per-CPI · 105 RF metadata (TWO docs per job: node 1001 tx / 1002 rx — query `node_id`) · 113 CALIBRATION_DATA · 134 INUSE_REF · 149 TRACKER_MODEL_DATA · `runcmd_<hex>` = pub-sub bus log | A "radar-side hole" may be jobs failing (8/28 redeploy w/o SDR power-cycle → 14/14 `RX_CPI_DATA_LOSS_ERROR`) | Check 136 before blaming the tracker; sdr `rx_overflow/rx_out_of_sequence` counters in healthStatus | `mru-job-failure-triage` |
| E4 | **DB-name trap** | `MongoClient(host)["run_..."]` is a DATABASE; `corr_lib.connect()` already returns the `sensor_store` DB so `db["sensor_store"][run]` silently queries an EMPTY collection (made `cache_dump_0827.py`'s cache empty) | `cl["sensor_store"]["run_<hex>"]` or `connect()[run_name]`; assert non-zero counts | 8/27 |
| E5 | **`runs` collection**: `run_id_str` = DASHLESS uuid; full config snapshot per run; `date_time` UTC; friendly names (8/25 Crimson_Panda, 8/26 Turquoise_Emu, 8/27 Ivory_Rattlesnake, 8/28 Beige_Badger) | Wrong run picked | `corr_lib.run_names`, `run_spans` ($natural probes, 25× faster over tailscale), `find_runs` | — |
| E6 | **Network**: mx (`10.1<MRU>.28.205`) and tx are tailscale SUBNET ROUTERS — mx down makes the whole `.28` side look dark; MRU91 Tx is on `.18.205`; tailscale SSH hangs (use LAN IP); mongo :27017 no-auth | "unit dead" | Check the router before declaring the unit dead; route MAVLink sender via a working path | `mru-opstate…`, `mru91-access-and-luks` |
| E7 | **Unit dies mid-session** | Data gone (8/27 MRU91 shutdown; only cache survives; NO 8/27 OBS outside cache windows) | Dump early and often (`quickdump.py HHMM HHMM name`, `seawall_archiver.py`); cache npz; report builders cache-first | 8/27 |
| E8 | **Official correlation fields** (`payload.truth_match`, `payload.contributors`, `observations[].truth_target`) were NOT archived | spa recorrelation impossible offline | Re-dump them when the unit is reachable; `ih/archive.py` now saves them | 9/10 |
| E9 | **Offline extracts**: `DataLoader(run_str, nas_folder=<root WITHOUT /mongo/db=sensor_store>)`; `JAX_PLATFORMS=cpu`; spa/chaotic UPPER-CASE target ids (`MAV14550_1_1`) or `prepare_grading_inputs` raises | Crashes / empty grading | `aoa_bias_correct.py`, `extract_filtered_track_states.py` template | — |
| E10 | **Rendering**: no Chrome/kaleido on the box — plotly can't rasterize headless (proxy-render matplotlib; headless Firefox for checks); plotly JSON escapes `/` as `/` so grep `data:image/jpeg` never matches an embedded satellite tile — grep `/jpeg` | "missing underlay" false alarms | — | 8/31 |

---

## F. Onboard-log (ulog / IR) alignment

| # | Item | Rule | Handled in | Seen |
|---|------|------|------------|------|
| F1 | **FC clock offset** — Zeus FC "UTC" + camera laptop ran **10h00m02s BEHIND UTC** (ZOFF +36002 s); Fox log_62 clock TRUE (don't shift) | Never trust FC/laptop clocks; verify by trajectory xcorr vs radar-side truth (2.5–6.4 m overlay proves Zeus = mav14551) | `make_plots.py`/`guidance_story.py` ZOFF | 8/27 |
| F2 | **Per-log zoff via horizontal-position xcorr** — 8/27 log_9 36002.4 / log_10 36002.0; 8/28 F1..F4 36001.3/.8/.7/.3 — never a shared constant | Clock method: horiz residual 2.4–4.7 m, alt σ 1.5–2.9 m. Takeoff-event alignment lands 0.3–2.9 s late (2 Hz feed + latency) → 5–81 m. **Use the clock method.** zoff is config DATA per engagement | `engagement_tab._ulog_series(zoff)`, `alt_compare.py` | 9/10 |
| F3 | **UTC anchor** = `vehicle_gps_position.time_utc_usec` − its `timestamp` column (NOT `boot_time_utc_us`); `ULog(file, msg_filter, disable_str_exceptions=True)` | — | `_ulog_series` (dynamic utc key) | 8/27 |
| F4 | **New PX4 GPS schema** `latitude_deg` / `altitude_ellipsoid_m` (HAE) vs legacy `lat·1e-7` / `alt_ellipsoid` mm; `altitude_msl_m` matches the feed's `alt_ft×0.3048` to 0±0.2 m; N = −29.4 | One parser for both | `_ulog_series` | 8/28 Zeus3 |
| F5 | **Fullwidth-colon filenames** (U+F03A, SMB-mangled) | Always `glob`; largest match wins (skips boot stubs) | `_ulog_series(ulog_path glob)` | 8/28 |
| F6 | **IR MKV broken index** (live-muxed, no keyframes, header frame counts garbage, no DateUTC) | cv2 random seek returns corrupt grey frames — decode SEQUENTIALLY only; count by decoding; anchor time from filename (`raw_camera_YYYYMMDD_HHMMSS.mkv`, LOCAL PDT) or IR motion-onset ↔ ulog climb | `_ir_extract`, `_fov_render` | 8/27 |
| F7 | **Camera time warp** — recorder stamps uniform 62 ms while camera drops/stretches frames → ±5 s drift WITHIN a video, worst during dashes | Any frame-time use MUST go through the per-frame warp (`camwarp_eng{1,2}.npz`, gyro↔egomotion windowed xcorr + content pass); single-lag constants are only averages; apply content shifts only with r > 0.5 | `cam_warp_fit.py`, `cam_warp_dive_fix.py`, `fov.cam` config | 8/28 |
| F8 | **FOV geometry** — boresight = body +X from quaternion (ENU), cone 6° half-angle / 500 m; interceptor position from ulog NOT feed (feed gaps smear dives); 5 Hz grid (event merge 0.7 s / drop 0.4 s tuned to 5 Hz — 10 Hz drops blip events); dash = `nav_state == 14` OFFBOARD; takeoff = sustained climb | 8/27: only 2 IN-FOV events (7 s, 5.8 s); 8/28: 62.4 s over 16 events, ranges 100–139 m | `build_fov_section`, `_fov_runs` | 8/27–28 |
| F9 | Misc: Zeus battery 0.0 V = no sense line (not anomaly); Fox zip was a byte-identical Zeus duplicate (md5) — check before target-side analysis; log_62 sys_id 13 | — | — | 8/27 |
| F10 | **Video serving** — `python -m http.server` has no HTTP Range support; cv2 has no H264 encoder | Player blob-loads the whole file (all seeks client-side); render MJPG intermediate → ffmpeg libx264 mp4 (or VP8 webm); mp4v does not play in browsers | `_FOVP_JS`, `_fov_render` | 8/28 |

---

## G. Interpretation traps

| # | Trap → rule |
|---|-------------|
| G1 | **Az-bias sign & cal link** — bias = median(track az − truth az) over fresh, moving, range > 300 m target-side samples; negative = track reads LOW. Week arc: −2.2 (8/25 pre-cal) → −1.45/−1.41 (8/26) → −0.19 (8/27) → −0.21 live / −0.45 recomputed (8/28). Present at BOTH obs and track level → sensor pointing, not filter. Cal-geometry fit: array physically **+2.26° CW** of model (rotating the rx array to minimise circular variance of measured−geometry phase) ⇒ yaw_offset fix ≈ −az_bias (−135° → ≈ −132.74°). Sign trap in the marimo helper: `wrapped_difference` = measured − geometry_raw while the displayed geometry is wrap(−raw); physical residual = measured + geometry_raw. At 2.5 km, 2.2° ≈ 95 m — essentially all of the 8/25 92–116 m error. |
| G2 | **El bias ≡ altitude datum** — +0.37–0.5° ≡ +20 m is TRACK-only (obs unbiased 0.0/+0.1°, σ 0.7°) → filter/100 m floor (C3), NOT a frame error. Any el/alt bias claim must first rule out A1 (feet) and A7 (geoid). Raw obs el is a range-dependent compression (C12), not a constant. |
| G3 | **Range-flat test** — a pure yaw error is range-flat. Az-vs-range slope (MRU43 +0.6–1.0°/km) ⇒ not pure yaw. Range: NO consistent bias on 8/26 (track F1 +24 m / F2 −5 m; scale 0.996–1.013) → never report a range bias from one flight/sign. |
| G4 | **Thresholded-bias rule** — report a bias only if \|median\| ≥ 0.5σ, above the floor, same sign in both flights; robust median ± σ, never RMSE (inflated by filter-initiation/turn transients). Headline stat elsewhere = 95.5 %-trimmed RMSE (spa). |
| G5 | **Heat-map scaling** — median cell ≈ 100 % active, so a linear scale washes out: colour by PERCENTILE of %-active with the bottom 15 % clamped solid red; score 30 m cells over a **±75 m (5×5) neighbourhood** (per-cell dwell = salt-and-pepper); hover keeps true active % + pXX; worst-cells table needs **≥ 300 m non-max suppression** + top-2 tracks per cell (else all rows sit in one pocket naming one long-coasting track); exclude days with 0 target-side tracks (8/25); hover excluded from the denominator; ≥ 4 truth samples per cell. |
| G6 | **Coverage vs identity** — see D5. Quote coverage with its definition (fresh ≤ 1.5 s, confirmed, moving > 2–2.5 m/s, target-side) — live and offline definitions differed (96 vs 91.4 %). |
| G7 | **Drop attribution** — NOT range (86–95 % active 1–4 km, no trend), NOT altitude (held at 4–19 m above antenna at 82–100 %). Red cells: turning ±3 s 37 %, pad/climb/descent 31 %, interceptor within 200 m 25 %, Doppler notch 4 %, straight cruise 3 %; long straight-leg drops = Delta filter (C4). The "low-alt eastern leg = LOS" narrative is WRONG for 8/28 (medU 75–78 m in every East band). Check the decomposition before writing a cause. |
| G8 | **Turn cost** — week 28.6 % coasting in turns vs 15.9 % straight (1.8×); 8/28 inverts (0.8×) — narrate why (holes on the straight leg). Turning = truth heading rate > 6°/s (3 s running median — GPS heading blips split legs), dilated ±3 s. |
| G9 | **Live quicklook numbers are provisional** — recompute offline on cleaned feeds; keep the live value in a note and mark non-reproduced passes `*` (8/28 F3 351 m, F2 428 m). |
| G10 | **Detections were present when tracks dropped** (8/26 ~2/s, SNR ~50 dB) → say "association failure", not "detection dropout", unless obs show a real gap (>19 s gaps at 08:49:23, 09:12:04 were true dropouts). |
| G11 | **Coverage inside idle periods is an artifact** (C14); radar detecting only in bursts on 8/27 pre-16:25 UTC was an ops fact, not data loss. |
| G12 | **Classification 'UAV' ≠ drone** (C7). Drone = kinematics + MAVLink truth match. |
| G13 | **Corrected accuracy is a derived quantity** — de-rotated (bias-removed) numbers (8/26 27/22 m; 8/28 22–29 m) may be quoted ONLY next to the RAW number with the bias stated (see report-rules: RAW by default). |
| G14 | **Trajectory mining** (`traj_mine.py`) — straights = heading rate < 3°/s sustained, speed ≥ 8 m/s, ≥ 12 s; clean = coasting ≤ 10 %, confirmed ≥ 90 %, RAW med err ≤ 120 m; trim at first anomaly (alt err > 50 m, alt jump > 15 m/sample, horiz spike > max(150 m, 2× own median — absolute gates wrongly kill bias-offset legs), position jump > 3·v·dt + 30 m), head-trim 10 s; on-plan = bearing ±5° of racetrack legs (NE 66.3° / SW 240.8°) and in-lane. Track U is a held prior, not information. |
